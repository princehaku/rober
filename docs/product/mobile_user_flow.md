# Mobile User Flow

## Minimum User Journey

1. User connects phone to the robot network or local control page.
2. User opens the local operator gateway.
3. User selects or confirms the trash station.
4. User places trash on the robot.
5. User taps start after confirming the load.
6. Robot announces or displays that it is preparing to depart.
7. Robot travels to the station.
8. Robot announces arrival and asks for removal or confirms dropoff.
9. User taps confirm after removing or disposing of the load.
10. Robot returns, waits, or reports that human help is required.

## Status States

- Waiting for trash.
- Loaded and ready.
- Delivering.
- Elevator assisted delivery states when enabled for a dry-run or controlled H2 route:
  - `approaching_elevator`
  - `waiting_elevator_open`
  - `entering_elevator`
  - `requesting_floor_help`
  - `waiting_target_floor`
  - `exiting_elevator`
  - `resume_delivery`
- Arrived at station.
- Returning.
- Completed.
- Needs human help.

## Exception Handling

The phone UI should show plain-language messages:

- "Route is not ready."
- "Robot cannot find the trash station."
- "Navigation failed."
- "电梯未开门，需要人工协助。"
- "未确认目标楼层，请人工确认。"
- "目标楼层证据不可靠，请人工确认。"
- "需要人工接管电梯段任务。"
- "Please remove trash manually."
- "Robot stopped for safety."

Users should not need SSH, ROS2 commands, serial tools, or direct hardware debugging for the normal flow.

## Minimum Local API

The first operator gateway is intentionally small: an API-first local HTTP service plus a minimal browser page at `/`. It is enough for a phone browser on the robot network, but it is not a polished native app, does not include account/login flows, and does not replace hardware bringup checks.

- `GET /api/status`
- `GET /api/diagnostics`
- `POST /api/collect`
- `POST /api/dropoff/confirm`
- `POST /api/cancel`

This is enough for a phone page or local browser control surface to complete a dry-run task and drive the manual dropoff confirmation service.

## Mobile Web Entrypoint

The standalone phone entrypoint now lives in `mobile/web/`:

- `index.html`, `styles.css`, and `app.js` implement a dependency-free phone-first shell.
- `manifest.webmanifest`, `service-worker.js`, `offline.html`, and SVG icons provide the minimal PWA shell.
- `mobile/fixtures/mobile_web_status.fixture.json` is marked as a static smoke fixture and is not live robot state.

This entrypoint consumes the existing phone-safe contract only. It reads `/api/status`, `/api/diagnostics`, `phone_readiness`, `command_safety`, and `phone_offline_resume_readiness`; it can render optional `phone_task_flow_readiness`, `phone_support_bundle`, and `voice_prompt_readiness` when they are present. It does not add robot state names or reinterpret ROS2 delivery semantics.

Start Delivery, Confirm Dropoff, and Cancel are disabled by default. The browser enables them only when `command_safety.actions.<action>.enabled=true` and the legacy permission fields also allow the same action. Blocked, offline, pending ACK, and manual-takeover states stay disabled. Diagnostics and Support Handoff remain visible while primary actions are blocked, because support collection must not require making motion-related controls available.

Start Delivery now has an explicit mobile task-start confirmation gate before the primary action button. The page must show the phone-safe target trash station or destination, require the user to check "trash loaded" manually, and show the current blocking reason. The destination can come from `phone_task_flow_readiness.destination_summary`, a `destination_confirmed` step, `phone_readiness.destination`, or `status.destination`; if none of those safe fields exist, Start remains disabled. This confirmation is a user action only, not automatic load detection.

`POST /api/collect` from `mobile/web/` is no longer body-less. The request body uses `schema=trashbot.mobile_task_start_confirmation.v1`, `schema_version=1`, `source=mobile_web`, a phone-safe `destination`, `target` with the same phone-safe destination for `/api/collect` compatibility, `trash_loaded_confirmed=true`, client timestamp/reference, `evidence_boundary=software_proof_docker_mobile_task_start_confirmation_gate`, and ACK semantics stating accepted/processing only. The payload must not include raw ROS topic names, `/cmd_vel`, serial devices, baudrate values, WAVE ROVER parameters, credentials, local paths, complete artifacts, or checksums.

The Start button is enabled only when all four gates pass: `command_safety.actions.start.enabled=true`, legacy `can_collect=true`, a safe destination is present, and the user has checked the load confirmation. Missing `command_safety`, missing destination, unchecked load confirmation, offline state, blocked state, pending ACK, or manual takeover all fail closed. ACK copy must remain visible as command accepted/processing evidence only and must not be described as delivery success.

The mobile PWA now makes that rule visible as a first-screen three-step primary journey summary:

1. Target trash station.
2. Manual trash-loaded confirmation.
3. Start safety gate.

This is `software_proof_docker_mobile_primary_journey_gate`. It proves only that the Docker/local `mobile/web/` shell and targeted unit test can render the primary journey, keep Start fail closed, and submit the existing `/api/collect` compatible `target` field inside a phone-safe confirmation envelope. It is not a real iPhone/Android test, not a production app, not a real PWA install prompt, not Nav2/fixed-route execution, not WAVE ROVER/HIL, and not delivery success.

The target trash station in this summary must come only from existing phone-safe fields: `phone_task_flow_readiness.destination_summary`, a confirmed `destination_confirmed` step, `phone_readiness.destination`, `status.destination`, or the same sanitized destination reused as `/api/collect.target` for compatibility. The page must not expose raw ROS topics, `/cmd_vel`, serial device names, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, local paths, tracebacks, checksums, or complete artifacts.

The trash-loaded step is a user confirmation only. The UI must not claim automatic load detection, automatic weighing, camera detection, or any sensor-derived proof of loaded trash unless a future backend contract explicitly provides that capability and the evidence boundary is updated.

The start safety gate must consume existing `phone_task_flow_readiness`, `command_safety`, cloud readiness, device/browser readiness, `operation_log` / `phone_operation_log`, and `mobile_action_receipt` / `phone_action_feedback`. Missing fields, blocked status, offline/unreachable state, pending ACK, manual takeover or human-help state, missing destination, and unchecked load confirmation all fail closed. ACK, HTTP accepted, and receipts remain accepted/processing evidence only; they do not prove delivery success, dropoff success, cancel completion, HIL, production readiness, or true robot motion.

`mobile_primary_journey_gate` and `mobile_primary_journey_summary` may appear in `/api/status` or `/api/diagnostics` as phone-safe support metadata. They are not robot commands, remote ACKs, cursors, delivery results, production readiness grants, or robot compatibility proof by themselves.

The mobile PWA now includes a read-only “电梯辅助状态” panel injected by `mobile/web/app.js` without changing the Start Delivery, Confirm Dropoff, or Cancel gating. It consumes `elevator_assist_rehearsal_evidence`, `elevator_assist_rehearsal_evidence_summary`, `elevator_assist`, `elevator_assist_summary`, or compatible summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or nested diagnostics summaries. The legacy phone-safe summary is `trashbot.elevator_assist_summary.v1`; the evidence-driven artifact schema is `trashbot.elevator_assist_rehearsal_evidence.v1`, and its Docker/local evidence boundary is `software_proof_docker_elevator_evidence_driven_mainline_gate`.

The panel shows only mode/status, safe `evidence_ref`, current phase, target floor, phase evidence summary, phase chain, human-help request, failure/manual-takeover reason, `same_evidence_ref_required`, `delivery_success=false`, `primary_actions_enabled=false`, evidence boundary, and `not_proven` list. The user-facing phase chain is: waiting for the elevator door, entering the elevator, asking a person to press the target floor, waiting for the target floor, exiting after the door opens, then resuming delivery. Failure copy must explain door-not-open, target-floor-not-confirmed, timeout, disabled warning, or manual takeover in Chinese-first language.

This is `software_proof_docker_elevator_evidence_driven_mainline_gate` or legacy `software_proof_docker_elevator_assist_default_mainline_gate` software proof only. It does not prove a real elevator ride, real speaker/TTS playback, real Nav2/fixed-route movement, WAVE ROVER/HIL, real dropoff/cancel completion, or delivery success. The panel must not expose raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, local paths, raw artifacts, complete artifacts, or checksums. Dry-run complete, ACK, artifact pass, and diagnostics pass must remain accepted/processing or software-proof metadata only, not delivery success.

The mobile PWA now includes a read-only “电梯现场材料校验” panel injected by `mobile/web/app.js` without changing Start Delivery, Confirm Dropoff, or Cancel authorization. It consumes `elevator_field_run_material_validation`, `elevator_field_run_material_validation_summary`, or compatible summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summaries. The expected artifact schema is `trashbot.elevator_field_run_material_validation.v1`, the expected summary schema is `trashbot.elevator_field_run_material_validation_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_elevator_field_material_validation_gate`.

The panel shows only validation status, safe `evidence_ref`, missing material categories, template/placeholder categories, mismatch categories, operator next steps, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. It is a support-facing material checklist for the next controlled elevator field run: door-state material, target-floor confirmation, human-assistance note, Nav2/fixed-route runtime log, task record, completion signal, and mobile/diagnostics safe summary must stay on the same `evidence_ref`.

This is software proof only. `blocked`, `ready_for_review`, or any material-validation status means only that phone-safe summaries can be reviewed; it is not a real elevator run, not HIL, not real Nav2/fixed-route execution, not dropoff/cancel completion, not delivery success, and not Objective 5 external proof. The panel must not expose raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, local paths, raw artifacts, complete artifacts, checksums, tracebacks, or success phrasing. It never fetches diagnostics by itself and never sends Start, Confirm, or Cancel.

The mobile PWA now includes a read-only “电梯现场复核决策” panel injected by `mobile/web/app.js` without changing Start Delivery, Confirm Dropoff, or Cancel authorization. It consumes `elevator_field_run_review`, `elevator_field_run_review_summary`, or compatible summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summaries. The expected artifact schema is `trashbot.elevator_field_run_review.v1`, the expected summary schema is `trashbot.elevator_field_run_review_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_elevator_field_review_decision_gate`.

The panel shows only `review_decision`, safe `evidence_ref`, blocked categories, operator next steps, commands to rerun, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. It translates the previous material-validation result into a phone-safe review handoff for support staff: missing materials, template materials, evidence-ref mismatch, unsafe copy, success-claim blocks, or invalid validation must remain blocked until the same `evidence_ref` is repaired and the review decision gate is rerun.

This review decision is software proof only. Decisions such as `blocked_template_materials`, `blocked_missing_materials`, or `ready_for_controlled_elevator_field_rehearsal_not_proven` do not prove a real elevator run, real Nav2/fixed-route execution, HIL, dropoff/cancel completion, delivery success, or Objective 5 external proof. The panel must not expose raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, local paths, raw review artifacts, raw validation artifacts, complete artifacts, checksums, tracebacks, or success phrasing. It never fetches diagnostics by itself and never sends Start, Confirm, or Cancel.

The mobile PWA now includes a read-only “电梯演练执行包” panel injected by `mobile/web/app.js` without changing Start Delivery, Confirm Dropoff, or Cancel authorization. It consumes `elevator_field_run_execution_pack`, `elevator_field_run_execution_pack_summary`, or compatible summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summaries. The expected artifact schema is `trashbot.elevator_field_run_execution_pack.v1`, the expected summary schema is `trashbot.elevator_field_run_execution_pack_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_elevator_field_rehearsal_execution_pack_gate`.

The panel shows only execution pack verdict, safe `evidence_ref`, controlled rehearsal manifest, required material templates, first-run command summary, rerun command summary, operator handoff, `safe_to_control=false`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. It is a support-facing handoff for the next controlled elevator rehearsal, not a control surface; the same `evidence_ref` must continue across door-state material, target-floor confirmation, human-assistance note, Nav2/fixed-route runtime log, task record, completion signal, diagnostics summary, and mobile safe summary.

This execution pack is software proof only. Verdicts such as `ready_for_controlled_rehearsal_not_proven` mean only that phone-safe rehearsal materials and commands are packaged for a human-controlled run. They do not prove a real elevator run, real Nav2/fixed-route execution, HIL, dropoff/cancel completion, delivery success, Objective 5 external proof, or real phone/browser validation. The panel must not expose raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, local paths, raw execution pack artifacts, complete artifacts, checksums, tracebacks, or success phrasing. It never fetches diagnostics by itself and never sends Start, Confirm, or Cancel.

The mobile PWA now includes a first-screen “恢复决策” panel immediately after the three-step primary journey. It prioritizes `mobile_recovery_decision_gate` and `mobile_recovery_decision_summary`, including compatible copies nested under `phone_readiness` or `/api/diagnostics`. When those fields are missing, the page derives only a blocked-by-design summary from existing phone-safe fields: offline/resume readiness, command safety, operation log, action feedback/receipt, support handoff, and primary journey readiness. It does not invent robot state.

The recovery decision schema is `trashbot.mobile_recovery_decision_gate.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_recovery_decision_gate`. The panel shows recovery state, recommended next action, blocking reason, support entry, ACK semantics, evidence boundary, and `not_proven` summary. It must give Chinese-first recovery advice for pending ACK, offline or stale status, manual takeover / human help, local submit failed, missing primary journey readiness, and missing support handoff.

The recovery decision panel is read-only. It must not call Start Delivery, Confirm Dropoff, Cancel, ACK routes, cursor updates, or robot command routes. It must not describe ACK, HTTP accepted, or receipt metadata as delivery success, dropoff success, cancel completed, production readiness, HIL, or true robot completion. Its local/static proof does not prove real iPhone/Android behavior, a production app, a real PWA install prompt, real HTTPS/TLS, real 4G/SIM, OSS/CDN live traffic, production DB/queue, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, real cancel completion, real dropoff completion, or real delivery.

The mobile PWA now includes a first-screen “路线/任务排练复盘” panel near the recovery and diagnostics surfaces. It consumes `route_task_rehearsal_operator_review`, `route_task_rehearsal_operator_review_summary`, or compatible route/task rehearsal summary fields from `/api/status`, `phone_readiness`, or `/api/diagnostics`. The page must not fetch raw artifacts, local filesystem paths, full execution bundles, backend files, checksums, or raw robot responses.

The panel shows only `overall_status`, `evidence_ref`, crosscheck/HIL boundary, mismatch summary, `next_rehearsal_decision`, `not_proven`, and `evidence_boundary=software_proof_docker_route_task_rehearsal_operator_review_gate`. Its copy/export path is whitelist-only: it may copy only the backend-provided `safe_copy`; if `safe_copy` is absent, the UI shows `blocked copy unavailable` and disables copy. This review is software proof only, not HIL, not real route execution, not dropoff/cancel completion, and not delivery success. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization.

The mobile PWA now includes a read-only “PC 路线调试 Console” availability panel. It consumes `pc_route_debug_console`, `pc_route_debug_console_summary`, or compatible route-debug-console summary fields from `/api/status`, `phone_readiness`, or `/api/diagnostics`; when missing it shows `not_proven` and does not fetch PC-local files, raw artifacts, local paths, route debug bundles, or task records.

The panel also reads the nested `pc_route_debug_console.route_elevator_reconciliation` safe summary when the PC console provides it. That nested field may show only reconciliation verdict/status, safe `evidence_ref`, missing material summary, mismatch summary, `delivery_success=false`, `primary_actions_enabled=false`, the source `evidence_boundary=software_proof_docker_elevator_route_evidence_reconciliation_gate`, the integration boundary `software_proof_docker_pc_route_elevator_console_integration_gate`, and `not_proven`. Missing nested summary keeps the PC panel at `blocked/not_proven`; the phone page must not fetch the raw elevator reconciliation artifact or PC-local route files.

The panel shows only availability, route debug status, recent task summary, route-elevator reconciliation summary, control boundary, `not_proven`, and `evidence_boundary=software_proof_docker_pc_route_debug_console_gate`. This is a support/diagnostics hint that PC route/task debug material exists; it is not the PC console itself, not HIL, not real route execution, not dropoff/cancel completion, and not delivery success. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization.

The mobile PWA now includes a read-only “路线电梯现场交接” panel injected by `mobile/web/app.js` without changing Start Delivery, Confirm Dropoff, or Cancel authorization. It consumes `route_elevator_field_session_handoff`, `route_elevator_field_session_handoff_summary`, or compatible summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_elevator_field_session_handoff.v1`, the expected summary schema is `trashbot.route_elevator_field_session_handoff_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_elevator_field_session_handoff_gate`.

The panel shows only handoff verdict, safe `evidence_ref`, same-evidence-ref required/status, required materials summary, operator next steps, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Required materials may be summarized as the next field-session checklist: `nav2_fixed_route_runtime_log.json`, `route_completion_signal.json`, `task_record.json`, `door_state.json`, `target_floor_confirmation.json`, `human_assistance_operator_note.md`, `dropoff_or_cancel_completion.json`, `delivery_result.json`, and `diagnostics_mobile_safe_summary.json`, all under the same safe `evidence_ref`.

This handoff is software proof only. `ready_for_field_session_handoff_not_proven` means only that the next field session has a phone-safe checklist and operator handoff; it does not prove a real route run, real elevator run, real dropoff/cancel completion, delivery success, HIL, or Objective 5 external proof. Missing summary renders `blocked/not_proven`. The panel must not fetch source files or expose sensitive internal details, external service materials, local paths, low-level control details, credentials, integrity metadata, or error stacks, and it never sends Start, Confirm Dropoff, Cancel, ACK, cursor, or robot command requests.

The local Chromium-family browser proof now includes this panel as `mobile_route_elevator_handoff_browser`. `pc-tools/evidence/phone_browser_acceptance_gate.py` must verify `routeElevatorFieldSessionHandoffTitle`, `routeElevatorFieldSessionHandoffBoundary`, and the fail-closed control copy in both `390x844` and `768x900` viewports while keeping Start Delivery, Confirm Dropoff, and Cancel disabled. This browser proof remains `software_proof_docker_route_elevator_field_session_handoff_gate` coverage for the read-only panel only; it is not real phone-device validation, real elevator/route execution, HIL, dropoff/cancel completion, delivery success, or Objective 5 external evidence.

The mobile PWA now includes a first-screen “路线电梯现场设备预检” panel for `mobile_route_elevator_field_device_precheck`. It consumes `mobile_route_elevator_field_device_precheck`, `mobile_route_elevator_field_device_precheck_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summary fields. The expected schema is `trashbot.mobile_route_elevator_field_device_precheck.v1`, the expected summary schema is `trashbot.mobile_route_elevator_field_device_precheck_summary.v1`, and the evidence boundary is `software_proof_docker_mobile_route_elevator_field_device_precheck_gate`.

The panel shows only route/elevator handoff reference, real device / PWA observation checklist summary, field material checklist summary, `real_device_observed=false`, `pwa_install_prompt_observed=false`, `route_elevator_field_pass=false`, `dropoff_completion=false`, `cancel_completion=false`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, precheck status, handoff reference, the conservative boolean fields above, checklist summaries, safe copy, recovery hint, evidence boundary, and `not_proven`.

This precheck is software proof only. It does not prove a real phone, real iPhone/Android device behavior, production app, real PWA prompt/user choice, real route/elevator field pass, dropoff completion, cancel completion, delivery success, HIL, or Objective 5 external proof. It never fetches raw diagnostics by itself and never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, or robot command requests; existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen “现场材料摄取” panel for `mobile_field_material_intake`, positioned after `mobile_route_elevator_field_device_precheck`. It consumes `mobile_field_material_intake`, `mobile_field_material_intake_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summary fields. The expected schema is `trashbot.mobile_field_material_intake.v1`, the expected summary schema is `trashbot.mobile_field_material_intake_summary.v1`, and the evidence boundary is `software_proof_docker_mobile_field_material_intake_gate`.

The panel shows only safe entry, safe `evidence_ref`, real device / PWA observation checklist summary, route/elevator field materials summary, same-evidence-ref status, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, intake status, safe entry, safe evidence ref, checklist summaries, same-evidence-ref status, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This intake is software proof only. It does not prove a real phone, real iPhone/Android device behavior, production app, real PWA prompt/user choice, real route/elevator field pass, real Nav2/fixed-route, dropoff completion, cancel completion, delivery success, HIL, or Objective 5 external proof. It never exposes raw ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, local paths, tracebacks, checksums, complete artifacts, raw robot responses, or raw field materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen “现场材料评审决策” panel for `mobile_field_material_review_decision`, positioned after `mobile_field_material_intake`. It consumes `mobile_field_material_review_decision`, `mobile_field_material_review_decision_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summary fields. The expected schema is `trashbot.mobile_field_material_review_decision.v1`, the expected summary schema is `trashbot.mobile_field_material_review_decision_summary.v1`, and the evidence boundary is `software_proof_docker_mobile_field_material_review_decision_gate`.

The panel shows only review decision, blocker summary, next-required-evidence summary, owner handoff, safe `evidence_ref`, same-evidence-ref status, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, review decision, blocker summary, next-required-evidence summary, owner handoff, safe evidence ref, same-evidence-ref status, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This review decision is software proof only. It does not prove a real phone, real iPhone/Android device behavior, production app, real PWA prompt/user choice, real route/elevator field pass, real Nav2/fixed-route, dropoff completion, cancel completion, delivery success, HIL, WAVE ROVER/UART behavior, or Objective 5 external proof. It never exposes raw artifacts, local paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, tracebacks, checksums, complete artifacts, raw robot responses, or raw field materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen “现场材料复测请求” panel for `mobile_field_material_retest_request`, positioned after `mobile_field_material_review_decision`. It consumes `mobile_field_material_retest_request`, `mobile_field_material_retest_request_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected schema is `trashbot.mobile_field_material_retest_request.v1`, the expected summary schema is `trashbot.mobile_field_material_retest_request_summary.v1`, and the evidence boundary is `software_proof_docker_mobile_field_material_retest_request_gate`.

The panel shows only source review decision, blockers, next-required-evidence, retest request, route/elevator material checklist, owner handoff, safe `evidence_ref`, same-evidence-ref status, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, source review decision, blockers summary, next-required-evidence summary, retest request summary, route/elevator material checklist summary, owner handoff, safe evidence ref, same-evidence-ref status, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This retest request is software proof only. It does not prove a real phone, real iPhone/Android device behavior, production app, real PWA prompt/user choice, real route/elevator field pass, real Nav2/fixed-route, dropoff completion, cancel completion, delivery success, HIL, WAVE ROVER/UART behavior, or Objective 5 external proof. It never exposes raw artifacts, local paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, tracebacks, checksums, complete artifacts, raw robot responses, raw review artifacts, raw retest artifacts, or raw field materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "硬件基线评审状态" panel for `hardware_baseline_review`, positioned after `mobile_field_material_retest_request`. It consumes `hardware_baseline_review`, `hardware_baseline_review_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The PC/Robot handoff artifact schema is `trashbot.hardware_baseline_review_gate.v1`, the Robot/mobile summary schema is `trashbot.hardware_baseline_review_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_baseline_review_gate`.

The panel shows only review status, product baseline, vendor coverage, `hardware_material_pending`, 2D LiDAR responsibility, ToF responsibility, safe `evidence_ref`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, review status, product baseline summary, vendor coverage summary, pending material summary, 2D LiDAR/ToF responsibility summaries, safe evidence ref, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This hardware baseline review is software proof only. It does not prove a real hardware baseline review, real 2D LiDAR material, real ToF safety-ring material, real vendor coverage, real Nav2/fixed-route, dropoff completion, cancel completion, delivery success, HIL, or Objective 5 external proof. It never exposes raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, hardware paths, credentials, DB/queue URLs, OSS AK/SK, tracebacks, checksums, complete artifacts, raw robot responses, raw baseline artifacts, or raw field materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "硬件基线来源对齐" panel for `hardware_baseline_source_alignment`, positioned after `hardware_baseline_review` and before the sensor procurement panels. It consumes `hardware_baseline_source_alignment`, `hardware_baseline_source_alignment_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected artifact schema is `trashbot.hardware_baseline_source_alignment.v1`, the summary schema is `trashbot.hardware_baseline_source_alignment_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_baseline_source_alignment_gate`.

The panel shows only `alignment_status`, default hardware set summary, target sensor baseline summary, vendor/source boundary, missing alignment items, safe `evidence_ref`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, alignment status, default hardware set summary, target sensor baseline summary, vendor/source boundary, missing alignment items, safe evidence ref, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This source alignment is software proof only. `hardware_baseline_source_alignment_not_proven` means the phone can show whether product hardware boundary, target sensor baseline, and `docs/vendor/VENDOR_INDEX.md` source boundary are aligned enough for review; it does not prove real 2D LiDAR / ToF source, receipt, installation, wiring, calibration, HIL entry, real Nav2/fixed-route, route/elevator field pass, dropoff completion, cancel completion, delivery success, or Objective 5 external proof. It never exposes raw vendor docs, raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, local paths, credentials, checksums, complete artifacts, raw robot responses, or success/control copy; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, diagnostics fetch, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "传感器采购摄取状态" panel for `hardware_sensor_procurement_intake`, positioned after `hardware_baseline_source_alignment`. It consumes `hardware_sensor_procurement_intake`, `hardware_sensor_procurement_intake_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected artifact schema is `trashbot.hardware_sensor_procurement_intake_gate.v1`, the summary schema is `trashbot.hardware_sensor_procurement_intake_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_sensor_procurement_intake_gate`.

The panel shows only procurement/intake status, missing source, missing procurement, missing mounting, missing wiring, missing calibration, missing HIL entry, owner next action, safe `evidence_ref`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, procurement status, missing-material summaries, owner next action, safe evidence ref, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This procurement intake is software proof only. `hardware_material_pending` means only that the phone can show a safe material intake checklist for 2D LiDAR / ToF follow-up; it does not prove real source material, real procurement, mounting, wiring, calibration, HIL entry, real Nav2/fixed-route, dropoff completion, cancel completion, delivery success, or Objective 5 external proof. It never exposes raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw vendor document, raw robot responses, or raw procurement materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, diagnostics fetch, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "传感器采购评审决策" panel for `hardware_sensor_procurement_review_decision`, positioned after `hardware_sensor_procurement_intake`. It consumes `hardware_sensor_procurement_review_decision`, `hardware_sensor_procurement_review_decision_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected artifact schema is `trashbot.hardware_sensor_procurement_review_decision.v1`, the summary schema is `trashbot.hardware_sensor_procurement_review_decision_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_sensor_procurement_review_decision_gate`.

The panel shows only source intake schema/status, `review_decision`, blocker summary, `next_required_evidence`, `owner_handoff`, safe `rerun_commands` summary, safe `evidence_ref`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, source intake status, review decision, blocker summary, next-required-evidence summary, owner handoff, rerun command summary, safe evidence ref, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This procurement review decision is software proof only. `hardware_material_pending` means only that the phone can show a safe procurement review handoff for 2D LiDAR / ToF follow-up; it does not prove real source material, real procurement, mounting, wiring, calibration, HIL entry, real Nav2/fixed-route, dropoff completion, cancel completion, delivery success, or Objective 5 external proof. It never exposes raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw vendor document, raw review artifact, raw robot responses, or raw procurement materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, diagnostics fetch, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "传感器采购执行包" panel for `hardware_sensor_procurement_execution_pack`, positioned immediately after `hardware_sensor_procurement_review_decision`. It consumes `hardware_sensor_procurement_execution_pack`, `hardware_sensor_procurement_execution_pack_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected artifact schema is `trashbot.hardware_sensor_procurement_execution_pack.v1`, the summary schema is `trashbot.hardware_sensor_procurement_execution_pack_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_sensor_procurement_execution_pack_gate`.

The panel shows only source review schema/decision, `execution_pack_status`, blocker, material templates, `owner_handoff`, safe `rerun_command` summary, safe `evidence_ref`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, source review decision, execution pack status, blocker, material templates, owner handoff, safe rerun command, safe evidence ref, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This procurement execution pack is software proof only. `hardware_material_pending` means only that the phone can show a safe execution handoff for 2D LiDAR / ToF procurement follow-up; it does not prove real source material, real procurement, mounting, wiring, calibration, HIL entry, real Nav2/fixed-route, dropoff completion, cancel completion, delivery success, or Objective 5 external proof. It never exposes raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw vendor document, raw execution pack, raw robot responses, or raw procurement materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, diagnostics fetch, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "传感器采购收货回填" panel for `hardware_sensor_procurement_receipt_intake`, positioned immediately after `hardware_sensor_procurement_execution_pack`. It consumes `hardware_sensor_procurement_receipt_intake`, `hardware_sensor_procurement_receipt_intake_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected artifact schema is `trashbot.hardware_sensor_procurement_receipt_intake.v1`, the summary schema is `trashbot.hardware_sensor_procurement_receipt_intake_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_sensor_procurement_receipt_intake_gate`.

The panel shows only source execution-pack schema, `receipt_intake_status`, `material_status`, missing material summary, accepted material summary, rejected material summary, `owner_handoff`, `next_required_evidence`, safe `evidence_ref`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, source execution-pack schema, receipt intake status, material status, missing/accepted/rejected material summaries, owner handoff, next required evidence, safe evidence ref, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This procurement receipt intake is software proof only. `hardware_material_pending` means only that the phone can show a safe receipt-intake handoff for 2D LiDAR / ToF material follow-up; it does not prove real receipt, real source material, real procurement completion, mounting, wiring, calibration, HIL entry, real Nav2/fixed-route, route/elevator field pass, dropoff completion, cancel completion, delivery success, or Objective 5 external proof. It never exposes raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw vendor document, raw receipt, raw robot responses, or raw procurement materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, diagnostics fetch, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "传感器 HIL 入口配置预检" panel for `hardware_sensor_hil_entry_config_precheck`, positioned immediately after `hardware_sensor_procurement_receipt_intake`. It consumes `hardware_sensor_hil_entry_config_precheck`, `hardware_sensor_hil_entry_config_precheck_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected artifact schema is `trashbot.hardware_sensor_hil_entry_config_precheck.v1`, the summary schema is `trashbot.hardware_sensor_hil_entry_config_precheck_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_sensor_hil_entry_config_precheck_gate`.

The panel shows only `precheck_status`, safe `evidence_ref`, sensor count summary, threshold summary, frame IDs summary, safety policy summary, missing config/material summary, `next_required_evidence`, `owner_handoff`, safe copy, evidence boundary, `not_proven`, `delivery_success=false`, and `primary_actions_enabled=false`. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, precheck status, safe evidence ref, sensor count summary, threshold summary, frame IDs summary, safety policy summary, missing config/material summary, next required evidence, owner handoff, safe copy, evidence boundary, `not_proven`, `delivery_success=false`, and `primary_actions_enabled=false`. If the backend summary has no `safe_copy`, the UI displays `blocked copy unavailable`.

This HIL-entry config precheck is software proof only. `hardware_sensor_hil_entry_config_precheck_not_proven` means only that the phone can show whether future HIL-entry sensor config has parameterized sensor count, threshold, frame ID, safety policy, and missing-material summaries. It does not prove real HIL, real 2D LiDAR / ToF, real phone/browser behavior, real Nav2/fixed-route, dropoff completion, cancel completion, delivery success, or Objective 5 external proof. It never exposes raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, local paths, tracebacks, checksums, complete artifacts, raw config, raw sensor config, raw robot responses, or raw hardware materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, diagnostics fetch, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "传感器 HIL 准入评审" panel for `hardware_sensor_hil_entry_readiness_review`, positioned immediately after `hardware_sensor_hil_entry_config_precheck`. It consumes `hardware_sensor_hil_entry_readiness_review`, `hardware_sensor_hil_entry_readiness_review_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected artifact schema is `trashbot.hardware_sensor_hil_entry_readiness_review.v1`, the summary schema is `trashbot.hardware_sensor_hil_entry_readiness_review_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_sensor_hil_entry_readiness_review_gate`.

The panel shows only readiness status, safe `evidence_ref`, missing materials, `next_required_evidence`, `owner_handoff`, boundary summary, evidence boundary, `not_proven`, `delivery_success=false`, and `primary_actions_enabled=false`. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, source schema, readiness status, safe evidence ref, missing-material summary, next-required-evidence summary, owner handoff, boundary summary, safe copy, evidence boundary, `not_proven`, `delivery_success=false`, and `primary_actions_enabled=false`.

This HIL-entry readiness review is software proof only. `hardware_sensor_hil_entry_readiness_review_not_proven` means only that the phone can show whether Hardware/Robot have provided enough sanitized material for a future HIL-entry review. It does not prove real HIL, real 2D LiDAR / ToF installation, real wiring, real power validation, real calibration, real phone/browser behavior, real Nav2/fixed-route, route/elevator field pass, dropoff completion, cancel completion, delivery success, or Objective 5 external proof. It never exposes raw vendor docs, raw ROS topics, `/cmd_vel`, raw JSON, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, absolute paths, tracebacks, checksums, complete artifacts, raw review artifacts, raw robot responses, or raw hardware materials; it also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, diagnostics fetch, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a first-screen "传感器 HIL 准入执行包" panel for `hardware_sensor_hil_entry_execution_pack`, positioned immediately after `hardware_sensor_hil_entry_readiness_review`. It consumes `hardware_sensor_hil_entry_execution_pack`, `hardware_sensor_hil_entry_execution_pack_summary`, or compatible phone-safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or status diagnostics summaries. The expected artifact schema is `trashbot.hardware_sensor_hil_entry_execution_pack.v1`, the summary schema is `trashbot.hardware_sensor_hil_entry_execution_pack_summary.v1`, and the evidence boundary is `software_proof_docker_hardware_sensor_hil_entry_execution_pack_gate`.

The panel shows only execution/readiness status, safe `evidence_ref`, required and missing material summaries, `next_required_evidence`, `owner_handoff`, `rerun_commands`, boundary summary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`. It is an operator handoff surface for preparing a future HIL-entry session; it does not expose raw materials, complete bundles, local filesystem locations, secret-bearing references, low-level device transport details, checksums, tracebacks, or completion claims.

This HIL-entry execution pack is software proof only. `hardware_sensor_hil_entry_execution_pack_not_proven` means only that the phone can show which sanitized materials and rerun summaries are still needed before a future Hardware/Robot review. It does not prove real HIL, real 2D LiDAR / ToF installation, real power validation, real calibration, real phone/browser behavior, real Nav2/fixed-route, route/elevator field evidence, dropoff outcome, cancel outcome, or Objective 5 external proof. It also never sends Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, diagnostics fetch, or robot command requests. Existing Start / Confirm Dropoff / Cancel gating remains fail-closed and unchanged.

The mobile PWA now includes a read-only “路线任务现场联跑准备” availability panel. It consumes `route_task_field_run_readiness` first, then `route_task_field_run_readiness_summary`, with compatible phone-safe summaries nested under `phone_readiness` or `/api/diagnostics`. Missing summaries display `blocked/not_proven`; the page does not fetch raw artifacts, local filesystem paths, complete execution bundles, checksums, tracebacks, raw robot responses, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, or OSS AK/SK.

The panel shows only readiness availability / overall status, `evidence_ref`, whether the same evidence ref is required, next evidence or missing material summary, phone-safe commands summary, `not_proven`, and `evidence_boundary=software_proof_docker_route_task_field_run_readiness_gate`. It is only a next field run readiness handoff for preparing the next route-task field run. It is not HIL, not real route execution, not real fixed-route/Nav2 proof, not delivery success, not dropoff/cancel completion, and not Objective 5 external proof. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization.

The mobile PWA now includes a read-only “路线任务现场材料复核” panel. It consumes `route_task_field_run_intake`, `route_task_field_run_intake_summary`, or compatible crosscheck summaries nested under `phone_readiness` or `/api/diagnostics`. The expected schema is `trashbot.route_task_field_run_intake_crosscheck.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_run_intake_crosscheck_gate`.

The intake crosscheck panel shows only status, safe `evidence_ref`, `missing_materials`, `mismatch_reasons`, `commands_to_rerun`, `not_proven`, and the evidence boundary. For diagnostics compatibility, it accepts both top-level `missing_materials` / `mismatch_reasons` / `commands_to_rerun` and nested `crosscheck.status`, `crosscheck.missing_materials`, `crosscheck.mismatch_reasons`, and `crosscheck.commands_to_rerun`. It does not fetch raw artifacts, complete execution bundles, local filesystem paths, checksums, tracebacks, raw robot responses, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This crosscheck is software proof only. `ready_for_review` means the field-run material shape is ready for human review; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not delivery success, not dropoff/cancel completion, not true robot motion, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never sends a robot command.

The mobile PWA now includes a read-only “路线任务现场复盘 Console” panel. It consumes `route_task_field_run_review`, `route_task_field_run_review_summary`, or compatible review summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or `diagnostics.diagnostics_summary`. The Robot diagnostics summary schema is `trashbot.route_task_field_run_review_summary.v1`; the upstream source artifact schema is `trashbot.route_task_field_run_review_console.v1`. The Docker/local evidence boundary remains `software_proof_docker_route_task_field_run_review_console_gate`.

The field-run review panel shows only `review_decision`, safe `evidence_ref`, `missing_materials` plus `mismatch_reasons`, `commands_to_rerun`, `operator_next_steps`, `not_proven`, and the evidence boundary. For diagnostics compatibility, it accepts both top-level fields and nested `crosscheck.status`, `crosscheck.missing_materials`, `crosscheck.mismatch_reasons`, and `crosscheck.commands_to_rerun`. It does not fetch raw artifacts, complete execution bundles, local filesystem paths, checksums, tracebacks, raw robot responses, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This review console is software proof only. A review decision such as `blocked_pending_operator_rerun` or `ready_for_review` means only that the field-run material summary is ready for operator review or rerun planning; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not delivery success, not dropoff/cancel completion, not true robot motion, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never sends a robot command.

The mobile PWA now includes a read-only “路线任务现场执行包” panel. It consumes `route_task_field_run_execution_pack`, `route_task_field_run_execution_pack_summary`, or compatible execution summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or `diagnostics.diagnostics_summary`. The expected artifact schema is `trashbot.route_task_field_run_execution_pack.v1`; summary compatibility may use `trashbot.route_task_field_run_execution_pack_summary.v1`. The Docker/local evidence boundary is `software_proof_docker_route_task_field_run_execution_pack_gate`.

The execution pack panel shows only `execution_status`, safe `evidence_ref`, `same_evidence_ref_required`, `required_materials`, first-run command summary, rerun command summary, `operator_next_steps`, `not_proven`, and the evidence boundary. It may read nested `field_run_manifest.required_materials`, `field_run_manifest.first_run_commands`, and `field_run_manifest.rerun_commands`, but it does not fetch raw artifacts, complete execution bundles, local filesystem paths, checksums, tracebacks, raw robot responses, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This execution pack is software proof only. `blocked_pending_same_evidence_ref_materials` or `ready_for_operator_execution` means only that the phone can show the next field-run material and command checklist; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not dropoff/cancel completion, not delivery success, not true robot motion, and not production cloud readiness. Missing summaries display blocked/not_proven. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never sends a robot command.

The mobile PWA now includes a read-only “路线任务现场复账” panel. It consumes `route_task_field_run_reconciliation`, `route_task_field_run_reconciliation_summary`, or compatible reconciliation summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_field_run_reconciliation.v1`; summary compatibility may use `trashbot.route_task_field_run_reconciliation_summary.v1`. The Docker/local evidence boundary is `software_proof_docker_route_task_field_run_reconciliation_gate`.

The reconciliation panel shows only `reconciliation_verdict`, safe `evidence_ref`, `same_evidence_ref_required`, `materials_status`, `operator_next_steps`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Missing summaries display blocked/not_proven. It does not fetch raw artifacts, complete execution bundles, local filesystem paths, checksums, tracebacks, raw robot responses, raw ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This reconciliation gate is software proof only. A verdict such as `blocked_pending_same_evidence_ref_materials` means only that phone-safe execution-pack and field-run materials can be compared by evidence ref; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not same-`evidence_ref` onboard reconciliation, not dropoff/cancel completion, not delivery success, not true robot motion, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never sends a robot command.

The mobile PWA now includes a read-only “路线现场证据包” panel. It consumes `route_task_field_run_evidence_kit`, `route_task_field_run_evidence_kit_summary`, or compatible evidence-kit summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_field_run_evidence_kit.v1`; summary compatibility may use `trashbot.route_task_field_run_evidence_kit_summary.v1`. The Docker/local evidence boundary is `software_proof_docker_route_task_field_run_evidence_kit_gate`.

The evidence-kit panel shows only `kit_verdict`, safe `evidence_ref`, material directory manifest summary, capture templates, commands to run/rerun, missing materials, operator handoff, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Missing summaries display blocked/not_proven. It does not fetch or render raw artifacts, complete bundles, local filesystem paths, tokens, credentials, raw ROS topics, `/cmd_vel`, serial/UART details, baudrate values, checksums, tracebacks, raw robot responses, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This evidence kit is software proof only. A verdict such as `blocked_pending_same_evidence_ref_materials` means only that the phone can show a safe material checklist and operator handoff for the next field run; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not same-`evidence_ref` onboard reconciliation, not dropoff/cancel completion, not delivery success, not true robot motion, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never sends a robot command.

The mobile PWA now includes a read-only “路线现场材料包” panel. It consumes `route_task_field_run_material_bundle`, `route_task_field_run_material_bundle_summary`, or compatible material-bundle summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_field_run_material_bundle.v1`; summary compatibility may use `trashbot.route_task_field_run_material_bundle_summary.v1`. The Docker/local evidence boundary is `software_proof_docker_route_task_field_run_material_bundle_gate`.

The 现场材料包 panel shows only bundle status, safe `evidence_ref`, template files, missing materials, operator next steps, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Missing summaries display blocked/not_proven. It does not fetch or render raw artifact contents, complete bundles, local filesystem paths, tokens, credentials, raw ROS topics, `/cmd_vel`, serial/UART details, baudrate values, checksums, tracebacks, raw robot responses, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This material bundle is software proof only. A status such as `blocked_pending_same_evidence_ref_materials` means only that the phone can show a safe evidence ref, template file checklist, material gaps, and operator next steps for preparing a field run; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not same-`evidence_ref` onboard reconciliation, not dropoff/cancel completion, not delivery success, not true robot motion, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never sends a robot command.

The mobile PWA now includes a read-only “路线材料校验” panel. It consumes `route_task_field_run_material_validation`, `route_task_field_run_material_validation_summary`, or compatible validation summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_field_run_material_validation.v1`; summary compatibility may use `trashbot.route_task_field_run_material_validation_summary.v1`. The Docker/local evidence boundary is `software_proof_docker_route_task_field_run_material_validation_gate`.

The 路线材料校验 panel shows only validation status, safe `evidence_ref`, missing materials, placeholder materials, mismatch materials, operator next steps, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Missing summaries display blocked/not_proven. It does not fetch or render raw validation artifacts, complete bundles, local filesystem paths, tokens, credentials, raw ROS topics, `/cmd_vel`, serial/UART details, baudrate values, checksums, tracebacks, raw robot responses, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This material validation gate is software proof only. A status such as `blocked_missing_field_run_materials` means only that the phone can show which route/task field-run materials are missing, still placeholder-based, or mismatched for the same evidence ref; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not same-`evidence_ref` onboard reconciliation, not dropoff/cancel completion, not delivery success, not true robot motion, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never sends a robot command.

The mobile PWA now includes a read-only “路线现场运行准备” panel. It consumes `route_task_field_run_console`, `route_task_field_run_console_summary`, or compatible console summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_field_run_console.v1`; summary compatibility may use `trashbot.route_task_field_run_console_summary.v1`. The Docker/local evidence boundary is `software_proof_docker_route_task_field_run_console_gate`.

The field-run console panel shows only `console_verdict`, safe `evidence_ref`, `field_run_plan`, `capture_checklist`, dropoff/cancel material status, `operator_next_steps`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Missing summaries display blocked/not_proven. It does not fetch raw artifacts, local filesystem paths, complete bundles, checksums, tracebacks, raw robot responses, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This field-run console gate is software proof only. A verdict such as `blocked_missing_route_task_field_run_console_summary` means only that the phone is waiting for a phone-safe field-run preparation summary; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not dropoff/cancel completion, not delivery success, not true robot motion, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, or cursor routes.

The mobile PWA now includes a read-only “路线任务完成信号” panel. It consumes `route_task_completion_signal`, `route_task_completion_signal_summary`, or compatible completion summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_completion_signal.v1`; summary compatibility may use `trashbot.route_task_completion_signal_summary.v1`. The Docker/local evidence boundary is `software_proof_docker_route_task_completion_signal_gate`.

The completion signal panel shows only `completion_verdict`, safe `evidence_ref`, `dropoff_completion`, `cancel_completion`, failure/recovery reason, `operator_next_steps`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Missing summaries display blocked/not_proven. It does not fetch raw artifacts, local filesystem paths, complete bundles, checksums, tracebacks, raw robot responses, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This completion signal is software proof only. A verdict such as `completed_not_proven` means only that Docker/local completion signal materials are shaped for human review; it is not HIL, not real route execution, not real fixed-route/Nav2 proof, not real dropoff completion, not real cancel completion, not delivery success, not true robot motion, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, or cursor routes.

The mobile PWA now includes a read-only “任务终态复账” panel near the route/task completion signal panel. It consumes `route_task_terminal_completion_rehearsal`, `route_task_terminal_completion_rehearsal_summary`, or compatible terminal rehearsal summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_terminal_completion_rehearsal.v1`, the expected summary schema is `trashbot.route_task_terminal_completion_rehearsal_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_terminal_completion_rehearsal_gate`.

The panel shows only terminal verdict, safe `evidence_ref`, dropoff/cancel material status, failure/recovery reason, operator next steps, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, terminal verdict, safe evidence ref, dropoff/cancel material status, failure/recovery reason, operator next steps, safe copy, recovery hint, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This terminal rehearsal is software proof only. A verdict such as `blocked_missing_real_terminal_materials_not_proven` means only that terminal fields are shaped for the next controlled field-session review; it is not real route execution, not real fixed-route/Nav2 proof, not real dropoff completion, not real cancel completion, not delivery success, not true robot motion, not real phone/browser validation, and not production cloud readiness. It must not expose raw JSON, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, local paths, checksums, complete artifacts, raw robot responses, or successful-delivery wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, or robot command routes.

The mobile PWA now includes a read-only “终态复核决策” panel positioned next to the terminal rehearsal panel. It consumes `route_task_terminal_review_decision`, `route_task_terminal_review_decision_summary`, or compatible review summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_terminal_review_decision.v1`, the expected summary schema is `trashbot.route_task_terminal_review_decision_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_terminal_review_decision_gate`.

The panel shows only `review_decision`, `decision_reason`, safe `evidence_ref`, `owner_handoff`, `next_required_evidence`, `field_retest_request_guidance`, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, review decision, decision reason, safe evidence ref, owner handoff, next required evidence, field retest request guidance, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This terminal review decision is software proof only. A decision such as `blocked_missing_real_terminal_materials_not_proven` means only that support staff can see why a controlled field retest is not ready and which same-`evidence_ref` materials remain required. It is not real route execution, not real fixed-route/Nav2 proof, not real dropoff completion, not real cancel completion, not true robot motion, not real phone/browser validation, and not production cloud readiness. It must not expose sensitive low-level debug material, credentials, local paths, complete artifacts, checksums, raw robot responses, or completion-claim wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “现场复测执行包” panel positioned after the terminal review decision panel. It consumes `route_task_field_retest_execution_pack`, `route_task_field_retest_execution_pack_summary`, or compatible safe summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.route_task_field_retest_execution_pack.v1`, the expected summary schema is `trashbot.route_task_field_retest_execution_pack_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_execution_pack_gate`.

The panel shows only execution status, safe `evidence_ref`, `same_evidence_ref_required`, required field materials summary, rerun command summary, operator handoff, field retest checklist, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata: schema, execution status, safe evidence ref, same evidence ref required, required materials summary, rerun command summary, operator handoff, checklist, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`.

This retest execution pack is software proof only. Status such as `blocked_missing_real_field_retest_materials_not_proven` means only that the next real field retest has a phone-safe execution pack outline; it is not real route execution, not real fixed-route/Nav2 proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, local paths, checksums, tracebacks, raw ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, Objective 5 external material, raw robot responses, or success wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “路线任务现场复测交接” panel positioned after the existing retest execution pack panel without changing that older panel's semantics. It consumes `route_task_field_retest_session_handoff`, `route_task_field_retest_session_handoff_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or `status.diagnostics.summary`. The expected artifact schema is `trashbot.route_task_field_retest_session_handoff.v1`, the expected summary schema is `trashbot.route_task_field_retest_session_handoff_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_session_handoff_gate`.

The panel shows only handoff status, safe `evidence_ref`, session owner, required field materials summary, rerun commands summary, operator next steps, safe copy status, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only the backend-provided `safe_copy` plus phone-safe metadata: schema, handoff status, safe evidence ref, session owner, required materials summary, rerun command summary, operator next steps, safe copy, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`. If `safe_copy` is absent or unsafe, the UI must show `blocked copy unavailable` and disable copy/export instead of synthesizing a package.

This session handoff is software proof only. Status such as `ready_for_field_retest_session_handoff_not_proven` means only that support staff can hand off the next controlled field session checklist; it is not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, raw JSON, low-level control or hardware communication fields, credentials, external service URLs, local paths, tracebacks, checksums, complete artifacts, raw robot responses, or success wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “路线任务现场复测结果入口” panel positioned after the existing `route_task_field_retest_session_handoff` panel without changing that handoff panel's semantics. It consumes `route_task_field_retest_result_intake`, `route_task_field_retest_result_intake_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, or `status.diagnostics.summary`. The expected artifact schema is `trashbot.route_task_field_retest_result_intake.v1`, the expected summary schema is `trashbot.route_task_field_retest_result_intake_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_result_intake_gate`. If the upstream PC gate produced the result-intake summary from `route_task_field_retest_review_result_handoff`, the phone surface still displays only the normalized result-intake summary fields and does not expose the upstream handoff artifact.

The panel shows only intake status, safe `evidence_ref`, material completeness, eight result material summaries, missing material list, operator next steps, safe copy status, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only the backend-provided `safe_copy` plus phone-safe metadata: schema, intake status, safe evidence ref, material completeness, result material summaries, missing materials, operator next steps, safe copy, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`. If `safe_copy` is absent or unsafe, the UI must show `blocked copy unavailable` and disable copy/export instead of synthesizing a package.

This result intake is software proof only. Status such as `blocked_missing_route_task_field_retest_result_materials_not_proven` means only that support staff can see which controlled field retest result materials remain missing, including `door_state`, `target_floor_confirmation`, and `human_assistance_note`; it is not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, raw JSON, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, local paths, tracebacks, checksums, complete artifacts, raw robot responses, or success wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “路线任务现场结果复账” panel positioned after `route_task_field_retest_result_intake`. It consumes `route_task_field_retest_result_reconciliation`, `route_task_field_retest_result_reconciliation_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_result_reconciliation_summary`. The expected artifact schema is `trashbot.route_task_field_retest_result_reconciliation.v1`, the expected summary schema is `trashbot.route_task_field_retest_result_reconciliation_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_result_reconciliation_gate`. When the upstream result-intake summary came from `route_task_field_retest_review_result_handoff`, the panel may show only safe lineage metadata: `source_review_result_handoff_schema`, `source_review_result_handoff_status`, `source_result_intake_schema`, `source_result_intake_status`, and `source_lineage_chain=source_review_result_handoff -> source_result_intake -> route_task_field_retest_result_reconciliation`.

The panel shows only reconciliation verdict, safe `evidence_ref`, safe lineage chain, source result-intake schema/status, source review-result-handoff schema/status, same evidence ref status, eight result material status, missing material list, mismatch reasons, operator next steps, safe copy status, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only the backend-provided `safe_copy` plus phone-safe metadata: schema, reconciliation verdict, safe evidence ref, safe lineage chain, source result-intake schema/status, source review-result-handoff schema/status, same evidence ref status, result material status, missing materials, mismatch reasons, operator next steps, safe copy, evidence boundary, `delivery_success=false`, `primary_actions_enabled=false`, and `not_proven`. If `safe_copy` is absent or unsafe, the UI must show `blocked copy unavailable` and disable copy/export instead of synthesizing a package.

This result reconciliation is software proof only. Verdicts such as `blocked_missing_route_task_field_retest_result_reconciliation_materials_not_proven` mean only that support staff can compare the same safe `evidence_ref`, eight field-result material summaries, mismatch reasons, and Robot diagnostics compatible summary. It is not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, raw JSON, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, local paths, tracebacks, checksums, complete artifacts, raw robot responses, raw reconciliation artifacts, or success wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “路线任务结果验收包” panel positioned after `route_task_field_retest_result_reconciliation`. It consumes `route_task_field_retest_result_acceptance_packet`, `route_task_field_retest_result_acceptance_packet_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_result_acceptance_packet_summary`. The expected artifact schema is `trashbot.route_task_field_retest_result_acceptance_packet.v1`, the expected summary schema is `trashbot.route_task_field_retest_result_acceptance_packet_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_result_acceptance_packet_gate`.

This result acceptance packet is software proof only. Status such as `blocked_missing_route_task_field_retest_result_acceptance_packet_not_proven` means only that support staff can read packet status, safe lineage from `route_task_field_retest_result_reconciliation`, eight result materials summary, missing items, owner handoff, rerun commands, and pass/fail criteria for the next field retest. The pass/fail criteria are forward-looking acceptance rules, not a claim that the field run passed. The panel keeps `not_proven`, `delivery_success=false`, and `primary_actions_enabled=false`; it is not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, raw JSON, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, local paths, tracebacks, checksums, complete artifacts, raw robot responses, raw acceptance packets, or success wording. If `safe_copy` is missing, copy/export stays blocked and displays `blocked copy unavailable`. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “路线任务结果回填” panel positioned after `route_task_field_retest_result_acceptance_packet`. It consumes `route_task_field_retest_result_acceptance_backfill`, `route_task_field_retest_result_acceptance_backfill_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_result_acceptance_backfill_summary`. The expected artifact schema is `trashbot.route_task_field_retest_result_acceptance_backfill.v1`, the expected summary schema is `trashbot.route_task_field_retest_result_acceptance_backfill_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_result_acceptance_backfill_gate`.

The acceptance backfill panel shows only `backfill_status`, source packet status, safe `evidence_ref`, material completeness, same-`evidence_ref` alignment, missing/rejected material categories, owner handoff, rerun commands, `not_proven`, `delivery_success=false`, `primary_actions_enabled=false`, and the evidence boundary. Copy/export is whitelist-only and may export only safe metadata when backend `safe_copy` exists; without `safe_copy`, the UI shows `blocked copy unavailable`. This panel is software proof only: it does not prove a field pass, delivery success, dropoff completion, cancel completion, real phone/browser validation, HIL, real route/elevator execution, or Objective 5 external proof. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, result-intake routes, or robot command routes.

The mobile PWA includes a read-only “现场材料包” panel positioned after `route_task_field_retest_result_acceptance_backfill` when that panel is present, otherwise after `route_task_field_retest_result_acceptance_packet` or `route_task_field_retest_result_reconciliation`. It consumes `route_task_field_retest_material_pack`, `route_task_field_retest_material_pack_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_material_pack_summary`. The expected artifact schema is `trashbot.route_task_field_retest_material_pack.v1`, the expected summary schema is `trashbot.route_task_field_retest_material_pack_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_material_pack_gate`.

The material pack panel shows only `pack_status`, safe `evidence_ref`, same-evidence-ref status, material completeness, eight material status summaries, missing materials, rejected materials, operator next steps, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata when backend `safe_copy` exists; without safe copy the UI shows `blocked copy unavailable` and disables copy/export.

This material pack is software proof only. Status such as `blocked_missing_route_task_field_retest_material_pack_not_proven` means only that field staff can see which controlled retest materials remain missing or rejected under the same safe `evidence_ref`; it is not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw paths, credentials, complete artifacts, tracebacks, checksums, raw artifacts, raw material packs, raw ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, raw robot responses, or success control wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “现场操作演练” panel positioned after `route_task_field_retest_material_pack`. It consumes `route_task_field_retest_operator_drill`, `route_task_field_retest_operator_drill_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_operator_drill_summary`. The expected artifact schema is `trashbot.route_task_field_retest_operator_drill.v1`, the expected summary schema is `trashbot.route_task_field_retest_operator_drill_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_operator_drill_gate`.

The operator drill panel shows only `drill_status`, safe `evidence_ref`, next command labels, missing material prompts, operator callback checklist, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe metadata when backend `safe_copy` exists; without safe copy the UI shows `blocked copy unavailable` and disables copy/export.

This operator drill is software proof only. Status such as `blocked_missing_route_task_field_retest_operator_drill_not_proven` means only that field staff can read which material-pack, result-intake, and result-reconciliation labels/checklist should be followed under the same safe `evidence_ref`; it is not real command execution, not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw JSON, raw artifacts, raw commands, raw paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, checksums, tracebacks, raw robot responses, or success control wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “现场复测演练控制台” panel positioned after `route_task_field_retest_operator_drill`. It consumes `route_task_field_retest_drill_console`, `route_task_field_retest_drill_console_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_drill_console_summary`. The expected artifact schema is `trashbot.route_task_field_retest_drill_console.v1`, the expected summary schema is `trashbot.route_task_field_retest_drill_console_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_drill_console_gate`.

The drill console panel shows only `console_status`, safe `evidence_ref`, safe checklist/copy, command labels, missing material prompts, operator callback checklist, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe summary fields when backend `safe_copy` exists; without safe copy the UI shows `blocked copy unavailable` and disables copy/export.

This drill console is software proof only. Status such as `blocked_missing_route_task_field_retest_drill_console_not_proven` means only that field staff can read the next material-pack, result-intake, result-reconciliation, missing-material, and callback checklist summaries under the same safe `evidence_ref`; it is not real command execution, not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, raw JSON, raw paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw robot responses, or success control wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “现场复测验收简报” panel positioned after `route_task_field_retest_drill_console`. It consumes `route_task_field_retest_acceptance_brief`, `route_task_field_retest_acceptance_brief_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_acceptance_brief_summary`. The expected artifact schema is `trashbot.route_task_field_retest_acceptance_brief.v1`, the expected summary schema is `trashbot.route_task_field_retest_acceptance_brief_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_acceptance_brief_gate`.

The acceptance brief panel shows only `acceptance_status`, safe `evidence_ref`, pass/fail criteria, required evidence packet, owner handoff, safe copy, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe summary fields when backend `safe_copy` exists; without safe copy the UI shows `blocked copy unavailable` and disables copy/export.

This acceptance brief is software proof only. Status such as `blocked_missing_route_task_field_retest_acceptance_brief_not_proven` means only that field staff can read the field acceptance criteria, required evidence packet, and owner handoff under the same safe `evidence_ref`; it is not real command execution, not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, raw JSON, raw paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw robot responses, or success control wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “现场证据包派发” panel positioned after `route_task_field_retest_acceptance_brief`. It consumes `route_task_field_retest_evidence_dispatch`, `route_task_field_retest_evidence_dispatch_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_evidence_dispatch_summary`. The expected artifact schema is `trashbot.route_task_field_retest_evidence_dispatch.v1`, the expected summary schema is `trashbot.route_task_field_retest_evidence_dispatch_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_evidence_dispatch_gate`.

The evidence dispatch panel shows only `dispatch_status`, safe `evidence_ref`, material owners, recommended filenames, backfill order, callback checklist, fail-closed rerun notes, required evidence packet, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe summary fields when backend `safe_copy` exists; without safe copy the UI shows `blocked copy unavailable` and disables copy/export.

This evidence dispatch is software proof only. Status such as `blocked_missing_route_task_field_retest_evidence_dispatch_not_proven` means only that field staff can see who owns each safe material, which safe filenames to prepare, the backfill order, callback checklist, and rerun notes under the same safe `evidence_ref`; it is not real command execution, not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, raw JSON, raw paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw robot responses, or success control wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “现场回执入口” panel positioned after `route_task_field_retest_evidence_dispatch`. It consumes `route_task_field_retest_callback_intake`, `route_task_field_retest_callback_intake_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_callback_intake_summary`. The expected artifact schema is `trashbot.route_task_field_retest_callback_intake.v1`, the expected summary schema is `trashbot.route_task_field_retest_callback_intake_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_callback_intake_gate`.

The callback intake panel shows only `intake_status`, safe `evidence_ref`, received filenames summary, missing materials, same-evidence-ref match result, next backfill action, callback checklist result, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Its copy/export path is whitelist-only and may export only phone-safe summary fields when backend `safe_copy` exists; without safe copy the UI shows `blocked copy unavailable` and disables copy/export.

This callback intake is software proof only. Status such as `blocked_missing_route_task_field_retest_callback_intake_materials_not_proven` means only that field staff callback metadata can be summarized and compared against the same safe `evidence_ref`; it is not real command execution, not real route execution, not real fixed-route/Nav2 proof, not real elevator proof, not real dropoff completion, not real cancel completion, not delivery success, not HIL, not real phone/browser validation, and not Objective 5 external proof. It must not expose raw artifacts, raw JSON, raw paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw robot responses, or success control wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “现场回执复核决策” panel positioned after `route_task_field_retest_callback_intake`. It consumes `route_task_field_retest_callback_review_decision`, `route_task_field_retest_callback_review_decision_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_callback_review_decision_summary`. The expected artifact schema is `trashbot.route_task_field_retest_callback_review_decision.v1`, the expected summary schema is `trashbot.route_task_field_retest_callback_review_decision_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_callback_review_decision_gate`.

The panel displays review decision, safe `evidence_ref`, source intake status, missing/backfill summary, same-evidence-ref verdict, next required evidence, result-intake readiness, owner handoff, evidence boundary, `not_proven`, `delivery_success=false`, and `primary_actions_enabled=false`. Decision strings such as `ready_for_result_intake`, `needs_material_backfill`, `evidence_ref_mismatch_rerun`, and `unsupported_callback_schema` only describe software-proof review routing. Copy/export remains whitelist-only and is driven by upstream `safe_copy`; it does not show raw artifacts, raw JSON, raw paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw robot responses, or success control wording.

This callback review decision is software proof only. `ready_for_result_intake` does not submit result intake, `needs_material_backfill` does not prove a field pass, and `evidence_ref_mismatch_rerun` / `unsupported_callback_schema` are fail-closed review states. The panel never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, robot command routes, result-intake routes, diagnostics fetches, or any route that could imply real dropoff, cancel completion, HIL, true phone/browser validation, Objective 5 external proof, or delivery success.

The mobile PWA now includes a read-only “现场复测结果交接” panel positioned after `route_task_field_retest_callback_review_decision`. It consumes `route_task_field_retest_review_result_handoff`, `route_task_field_retest_review_result_handoff_summary`, or compatible Robot diagnostics summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, `diagnostics.diagnostics_summary`, nested diagnostics summary fields, `status.diagnostics.summary`, or `robot_diagnostics_route_task_field_retest_review_result_handoff_summary`. The expected artifact schema is `trashbot.route_task_field_retest_review_result_handoff.v1`, the expected summary schema is `trashbot.route_task_field_retest_review_result_handoff_summary.v1`, and the Docker/local evidence boundary is `software_proof_docker_route_task_field_retest_review_result_handoff_gate`.

The panel displays handoff status, safe `evidence_ref`, source review decision, result-intake readiness, required materials, owner handoff, blocked reasons, evidence boundary, `not_proven`, `delivery_success=false`, and `primary_actions_enabled=false`. It is a phone-safe handoff surface only: source review decisions such as `needs_material_backfill` and readiness strings such as `ready_for_result_intake=false` do not submit result intake, create ACKs, open cursors, or issue robot commands.

This review-result handoff is software proof only. It does not prove real field retest materials, real route/elevator field pass, real Nav2/fixed-route execution, real task record, real dropoff/cancel completion, HIL, real phone/browser validation, Objective 5 external proof, true delivery success, or any production result. Copy/export remains whitelist-only and is driven by upstream `safe_copy`; it must not expose raw artifacts, raw JSON, raw paths, credentials, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, DB/queue URLs, OSS AK/SK, checksums, complete artifacts, raw robot responses, ACK payloads, cursors, or success control wording. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, result-intake routes, ACK routes, cursor routes, diagnostics fetches, or robot command routes.

The mobile PWA now includes a read-only “电梯路线证据复账” panel. It consumes `elevator_route_evidence_reconciliation`, `elevator_route_evidence_reconciliation_summary`, or compatible summaries from `/api/status`, `phone_readiness`, `/api/diagnostics`, `diagnostics.summary`, or nested diagnostics summary fields. The expected artifact schema is `trashbot.elevator_route_evidence_reconciliation.v1`; summary compatibility may use `trashbot.elevator_route_evidence_reconciliation_summary.v1`. The Docker/local evidence boundary is `software_proof_docker_elevator_route_evidence_reconciliation_gate`.

The panel shows only `reconciliation_verdict`, safe `evidence_ref`, `same_evidence_ref_required`, `same_evidence_ref_status`, source states, missing material summary, mismatch summary, operator next steps, `delivery_success=false`, `primary_actions_enabled=false`, `not_proven`, and the evidence boundary. Missing summaries display blocked/not_proven. It does not fetch raw artifacts, local filesystem paths, complete bundles, checksums, tracebacks, raw robot responses, ROS topics, `/cmd_vel`, serial/UART details, baudrate values, WAVE ROVER parameters, credentials, DB/queue URLs, OSS AK/SK, or Objective 5 external proof material.

This elevator-route reconciliation panel is software proof only. A verdict such as `blocked_missing_same_evidence_ref_materials` means only that Docker/local summaries can show whether elevator rehearsal evidence and route completion material share the same safe `evidence_ref`; it is not a real elevator run, not real fixed-route/Nav2 execution, not HIL, not dropoff/cancel completion, not delivery success, and not production cloud readiness. It never changes Start Delivery, Confirm Dropoff, or Cancel authorization and never calls `/api/collect`, `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, or diagnostics fetches by itself.

The mobile PWA now includes a visible “终端动作二次确认” gate for Confirm Dropoff and Cancel. The first tap on Confirm Dropoff or Cancel must only open this panel; it must not call `/api/dropoff/confirm`, `/api/cancel`, ACK routes, cursor routes, or robot command routes. The panel shows the selected action, user risk copy, ACK semantics, `not_proven`, `evidence_boundary=software_proof_docker_mobile_terminal_action_confirmation_gate`, a generated `client_reference` preview, and a return button. Returning from the panel clears only local pending confirmation and submits nothing.

The terminal action confirmation gate is fail closed unless all existing safety inputs agree: `command_safety` must exist, `command_safety.actions.confirm_dropoff.enabled` or `command_safety.actions.cancel.enabled` must be true, legacy `can_confirm_dropoff` or `can_cancel` must be true, cloud/device/browser readiness must explicitly allow control, status must not be offline or stale, there must be no pending ACK, manual takeover / human help must not be active, blocked state must be absent, and the most recent action feedback must not be failed, blocked, rejected, or still accepted/processing. Missing fields are not interpreted as safe.

Only the explicit confirm button inside the terminal panel may submit the existing endpoint. The request body keeps the compatible `trashbot.mobile_action_confirmation.v1` envelope with `schema_version=1`, `source=mobile_web`, `action`, `user_confirmed=true`, `client_reference`, `client_timestamp`, Chinese-first `safe_phone_copy`, accepted/processing-only `ack_semantics`, and `evidence_boundary=software_proof_docker_mobile_terminal_action_confirmation_gate`. `mobile_terminal_action_confirmation_gate` and `mobile_terminal_action_confirmation_summary` are phone/support metadata only. They are not robot commands, ACKs, cursors, delivery results, dropoff success, cancel completion, production readiness, HIL, or real delivery proof.

The mobile PWA now includes a first-screen “手机设备证据采集” panel. It is a phone-safe evidence package collection surface for the step before real phone/browser/PWA acceptance, not proof that a real iPhone, Android phone, production app, or PWA install prompt has passed. The panel can consume `mobile_device_evidence_capture`, `mobile_device_evidence_capture_summary`, or `mobile_device_evidence_package` from `/api/status`, `phone_readiness`, or `/api/diagnostics`; when those fields are missing it only collects current browser metadata that is safe to copy.

The evidence capture schema is `trashbot.mobile_device_evidence_capture.v1`, the summary schema is `trashbot.mobile_device_evidence_capture_summary.v1`, the copy package schema is `trashbot.mobile_device_evidence_package.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_device_evidence_capture_gate`. The package may include viewport CSS width/height, device pixel ratio, orientation, touch target metadata, display-mode, manifest link presence, install prompt status, production app readiness, service worker/offline shell status, client timestamp, safe copy, recovery hint, ACK semantics, evidence boundary, and `not_proven`.

The copy package must be filtered to phone-safe metadata only. It must not include token, Authorization, OSS AK/SK, root password, DB/queue URL, ROS topic, `/cmd_vel`, serial, WAVE ROVER parameters, local filesystem paths, traceback, checksum, complete evidence files, raw robot responses, or robot/internal technical fields. Missing or blocked real-device evidence keeps Start Delivery, Confirm Dropoff, and Cancel fail closed through the existing readiness, browser acceptance bundle, and command safety gates. ACK, HTTP accepted, receipt, or the copied evidence package must remain accepted/processing or reproduction metadata only; it must not be described as delivery success, dropoff success, cancel completed, true phone acceptance, HIL, or real delivery.

The mobile PWA now includes a first-screen “真实手机验收交接会话” panel. It is the handoff surface for a real phone acceptance session, not proof that the real session has passed. The panel may consume `mobile_device_handoff_session`, `mobile_device_handoff_session_summary`, or `mobile_device_handoff_package` from `/api/status`, `phone_readiness`, or `/api/diagnostics`; when those fields are missing it derives a blocked-by-design handoff session from the current entry URL, local phone-safe metadata, `mobile_device_evidence_capture`, device acceptance readiness, and the browser acceptance bundle.

The handoff session schema is `trashbot.mobile_device_handoff_session.v1`, the copy package schema is `trashbot.mobile_device_handoff_package.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_device_handoff_session_gate`. The panel shows the current entry URL or safe entry summary, `session_id`, `client_reference`, the linked `mobile_device_evidence_capture` / `mobile_device_evidence_package` reference, a real-phone acceptance checklist, device/browser/PWA/install prompt/offline shell/touch target/viewport observation items, ACK semantics, evidence boundary, and `not_proven`.

The handoff copy package must be filtered to phone-safe metadata only. It may include session schema/version, session id, client reference, entry URL or safe entry summary, overall status, real-device observed flag, production app readiness, real PWA install prompt observed flag, safe-to-control flag, observation checklist, sanitized evidence-capture reference, sanitized device observations, sanitized browser-acceptance reference, safe copy, recovery hint, ACK semantics, evidence boundary, and `not_proven`. It must not include token, Authorization, OSS AK/SK, root password, DB/queue URL, ROS topic, `/cmd_vel`, serial, WAVE ROVER parameters, local filesystem paths, traceback, checksum, complete evidence files, raw robot responses, or robot/internal technical fields.

The handoff session deliberately reuses or references `mobile_device_evidence_capture`, but that capture package remains reproduction metadata only. It must not be described as real iPhone/Android behavior, production app readiness, true browser acceptance, a real PWA install prompt, HIL, or delivery success. Start Delivery, Confirm Dropoff, and Cancel remain fail closed unless the handoff session explicitly reports `safe_to_control=true`, `real_device_observed=true`, `production_app_ready=true`, `pwa_install_prompt_observed=true`, and `overall_status!=blocked`, and all existing command safety, cloud/device/browser readiness, operation log, and action feedback gates also pass.

The evidence boundary is `software_proof_docker_mobile_device_handoff_session_gate`. It proves only Docker/local mobile software proof for the handoff session surface and copy package. It does not prove real iPhone/Android device behavior, production app behavior, a real PWA install prompt, true public HTTPS/TLS, 4G/SIM, OSS/CDN live traffic, production DB/queue, Nav2/fixed-route, WAVE ROVER, HIL, real dropoff/cancel completion, or real delivery. ACK, HTTP accepted, receipt, evidence capture, and handoff session metadata remain accepted/processing/support metadata only.

The mobile PWA now includes a first-screen “PWA 安装提示证据” panel. It is the support and acceptance evidence surface for install prompt capture status and user outcome, not proof that a real prompt appeared or that a real user installed the app. The panel may consume `mobile_pwa_install_prompt_evidence`, `mobile_pwa_install_prompt_evidence_summary`, or `mobile_pwa_install_prompt_evidence_package` from `/api/status`, `phone_readiness`, or `/api/diagnostics`; when those fields are missing it derives only blocked-by-design metadata from the current static shell, `mobile_device_handoff_session`, `mobile_device_evidence_capture`, and `mobile_browser_acceptance_bundle`.

The install prompt evidence schema is `trashbot.mobile_pwa_install_prompt_evidence.v1`, the summary schema is `trashbot.mobile_pwa_install_prompt_evidence_summary.v1`, the copy package schema is `trashbot.mobile_pwa_install_prompt_evidence_package.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_pwa_install_prompt_evidence_gate`. The panel shows install prompt capture status, install prompt user outcome, display-mode, installability status, offline shell status, manifest/service worker status, production app readiness, safe-to-control, ACK semantics, evidence boundary, and `not_proven`.

The copy package must be filtered to phone-safe metadata only. It may include schema/version, source, overall status, install prompt capture status, user outcome, display-mode, installability status, offline shell status, manifest present, service worker status, production app readiness, safe-to-control, sanitized handoff/session reference, sanitized device-evidence reference, sanitized browser-acceptance reference, safe copy, recovery hint, ACK semantics, evidence boundary, and `not_proven`. It must not include token, Authorization, OSS AK/SK, root password, DB/queue URL, raw ROS topic, `/cmd_vel`, serial, WAVE ROVER, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, or robot/internal technical fields.

The install prompt evidence panel deliberately reuses or references `mobile_device_handoff_session`, `mobile_device_evidence_capture`, and `mobile_browser_acceptance_bundle`, but those references remain reproduction/support metadata only. They must not be described as real PWA install prompt proof. Missing real phone/browser behavior, production app behavior, true install prompt capture, or a real user outcome keeps Start Delivery, Confirm Dropoff, and Cancel fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, and action feedback gates. The new install prompt evidence package does not add a new control-grant condition.

The evidence boundary is `software_proof_docker_mobile_pwa_install_prompt_evidence_gate`. It proves only Docker/local mobile software proof for the install prompt evidence surface and copy package. It does not prove real iPhone/Android device behavior, production app behavior, a real PWA install prompt, a real install prompt user outcome, true public HTTPS/TLS, 4G/SIM, OSS/CDN live traffic, production DB/queue, Nav2/fixed-route, WAVE ROVER, HIL, real dropoff/cancel completion, or real delivery. ACK, HTTP accepted, receipt, evidence package, handoff session, and install prompt metadata remain accepted/processing/support metadata only.

The mobile PWA now captures PWA install prompt browser events as `mobile_pwa_install_prompt_event_capture`, `mobile_pwa_install_prompt_event_capture_summary`, and `mobile_pwa_install_prompt_event_capture_copy`. The schema is `trashbot.mobile_pwa_install_prompt_event_capture.v1`, the summary schema is `trashbot.mobile_pwa_install_prompt_event_capture_summary.v1`, the copy schema is `trashbot.mobile_pwa_install_prompt_event_capture_copy.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_pwa_install_prompt_event_capture_gate`. The static entrypoint listens for `beforeinstallprompt`, `appinstalled`, and `beforeinstallprompt.userChoice`. It records only observed/missing/blocked event state, client timestamps, display-mode, platform/browser summary, prompt availability, allowlisted userChoice outcome (`accepted`, `dismissed`, `unknown`, `not_proven`), appinstalled observed/timestamp, safe copy, recovery hint, ACK semantics, evidence boundary, and `not_proven`.

The event capture copy package is whitelist-only. It must not include raw Promise, raw browser event, complete user agent, token, Authorization, OSS AK/SK, DB/queue URL, credential-bearing URL, raw ROS topic, `/cmd_vel`, serial/UART, baudrate, WAVE ROVER parameters, local paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, or robot/internal technical fields. A deferred install prompt may be kept only in runtime memory so the page can observe `userChoice`; it must not be serialized into the copy package. Event capture never enables Start Delivery, Confirm Dropoff, or Cancel, and it is not production app readiness, real phone acceptance, O5 external proof, HIL, dropoff/cancel completion, delivery success, or real robot control proof.

The mobile PWA now exports PWA install prompt acceptance material as `mobile_pwa_install_prompt_evidence_export`, `mobile_pwa_install_prompt_evidence_export_summary`, and `mobile_pwa_install_prompt_evidence_export_copy`. The schema is `trashbot.mobile_pwa_install_prompt_evidence_export.v1`, the summary schema is `trashbot.mobile_pwa_install_prompt_evidence_export_summary.v1`, the copy schema is `trashbot.mobile_pwa_install_prompt_evidence_export_copy.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_pwa_install_prompt_evidence_export_gate`. Input priority is explicit export fields first, then `mobile_pwa_install_prompt_event_capture*`, then existing `mobile_pwa_install_prompt_evidence*`, `mobile_device_handoff_session*`, `mobile_device_evidence_capture*`, and `mobile_browser_acceptance_bundle*`; if none exist, the page derives a blocked-by-design export.

The export copy and download package is whitelist-only and uses the same JSON payload for both paths. It may include only `schema=trashbot.mobile_pwa_install_prompt_evidence_export_copy.v1`, `schema_version=1`, `source=mobile_web`, `overall_status`, `install_prompt_capture_status`, `install_prompt_user_choice`, `appinstalled_status`, `display_mode`, `manifest_present`, `service_worker_status`, `client_reference`, `client_timestamp`, `safe_to_control=false`, `ack_semantics=accepted_processing_only_not_delivery_success`, `evidence_boundary=software_proof_docker_mobile_pwa_install_prompt_evidence_export_gate`, `not_proven`, and `safe_phone_copy`. It must not include token, Authorization, OSS AK/SK, root password, DB/queue URL, credential-bearing URL, raw ROS topic, `/cmd_vel`, serial/UART, baudrate, WAVE ROVER, local filesystem path, traceback, checksum, complete artifact, raw robot response, raw event payload, or robot/internal technical fields. Export material remains support/acceptance metadata only and never enables Start Delivery, Confirm Dropoff, or Cancel.

The mobile PWA now includes a first-screen “真实设备验收材料” panel. It is an intake and redaction surface for real iPhone/Android, production app, screenshot summary, URL summary, observer note, browser summary, viewport/DPR/orientation, display-mode, PWA install prompt status/user choice, and production app readiness. It may consume `mobile_real_device_evidence_intake`, `mobile_real_device_evidence_intake_summary`, or `mobile_real_device_evidence_package` from `/api/status`, `phone_readiness`, or `/api/diagnostics`; it can also import a user-provided JSON summary through the text box. When no trusted summary exists it generates a local blocked-by-design package from current browser metadata.

The intake schema is `trashbot.mobile_real_device_evidence_intake.v1`, the summary schema is `trashbot.mobile_real_device_evidence_intake_summary.v1`, the copy package schema is `trashbot.mobile_real_device_evidence_package.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_evidence_intake_gate`. The copied package is always a redacted phone-safe package and keeps `safe_to_control=false`. It includes only device/platform summaries, browser family/version summary, viewport, DPR, orientation, display-mode, PWA install prompt status/user choice, production app readiness/release summary, screenshot summary, URL summary, observer note, redaction status, ACK semantics, evidence boundary, and `not_proven`.

The real-device evidence intake must filter token, Authorization, OSS AK/SK, root password, DB/queue URL, raw ROS topic, `/cmd_vel`, serial, baudrate, WAVE ROVER, local filesystem paths, traceback, checksum, complete artifacts, raw robot response, and robot/internal technical fields. A sensitive import is rejected and replaced with a blocked redaction failure summary. This gate is not a control grant: Start Delivery, Confirm Dropoff, and Cancel remain controlled only by the existing command safety, cloud/device/browser readiness, handoff session, operation log, and action-feedback gates. Missing, blocked, or `not_proven` intake material must not make actions green. ACK, HTTP accepted, evidence package, and observer note remain accepted/processing/support metadata only, not delivery success, production app readiness, PWA install prompt success, HIL, or real delivery proof.

The mobile PWA now includes a first-screen “真实设备验收决策” panel. It consumes or derives `mobile_real_device_acceptance_decision`, `mobile_real_device_acceptance_decision_summary`, and `mobile_real_device_acceptance_decision_package` from the previous `mobile_real_device_evidence_intake` / summary / package. The decision schema is `trashbot.mobile_real_device_acceptance_decision.v1`, the summary schema is `trashbot.mobile_real_device_acceptance_decision_summary.v1`, the copy package schema is `trashbot.mobile_real_device_acceptance_decision_package.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_acceptance_decision_gate`.

The decision gate has exactly three phone-safe decision outcomes: `accepted_for_review`, `blocked_missing_evidence`, and `rejected_unsafe_or_unredacted`. `accepted_for_review` means only that the redacted package can be reviewed by a human; it does not prove real iPhone/Android behavior, production app readiness, a real PWA install prompt, HIL, dropoff success, cancel completion, or delivery success. Missing real device behavior, production app release summary, real PWA install prompt capture/user choice, redacted screenshot summary, or production HTTPS URL summary keeps the decision at `blocked_missing_evidence`. Unsafe or unredacted input keeps the decision at `rejected_unsafe_or_unredacted`.

The decision package is filtered to phone-safe metadata only. It may include decision, accepted-for-review flag, `safe_to_control=false`, blocker list, next required evidence, redaction status, sanitized intake-package reference, safe phone copy, recovery hint, ACK semantics, evidence boundary, source evidence boundary, and `not_proven`. It must not include raw intake JSON, tokens, Authorization, OSS AK/SK, root password, DB/queue URL, raw ROS topic, `/cmd_vel`, serial, baudrate, WAVE ROVER, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, or robot/internal technical fields.

This decision gate is not a control grant. Start Delivery, Confirm Dropoff, and Cancel remain controlled only by command safety, cloud/device/browser readiness, handoff session, operation log, and action feedback. ACK, HTTP accepted, receipt, intake package, and decision package remain accepted/processing/support metadata only, not production app readiness, true PWA install prompt success, real phone-device acceptance, HIL, or real delivery proof.

The mobile PWA now includes a first-screen “真实设备评审交接” panel. It consumes or derives `mobile_real_device_review_handoff`, `mobile_real_device_review_handoff_summary`, and `mobile_real_device_review_handoff_package` from `mobile_real_device_acceptance_decision`, `mobile_real_device_acceptance_decision_summary`, or `mobile_real_device_acceptance_decision_package`. The review handoff schema is `trashbot.mobile_real_device_review_handoff.v1`, the summary schema is `trashbot.mobile_real_device_review_handoff_summary.v1`, the copy package schema is `trashbot.mobile_real_device_review_handoff_package.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_review_handoff_gate`.

The review handoff panel shows the reviewer checklist, decision status, review owner/status, evidence blocker, next required evidence, redaction status, source boundary, ACK-not-delivery wording, and `not_proven`. `review_owner` is an accountability field for human review routing, not a robot control principal. `review_status=ready_for_manual_review` means only that a human can inspect the redacted materials; it is not real-device acceptance and it is not a delivery or production-app signal.

The review handoff package is filtered to phone-safe metadata only. It may include handoff/session schema, handoff session id, decision status, review owner/status, `safe_to_control=false`, evidence blocker, next required evidence, reviewer checklist, redaction status, sanitized linked acceptance decision summary, safe phone copy, recovery hint, ACK semantics, evidence boundary, source evidence boundary, and `not_proven`. It must not include token, Authorization, OSS AK/SK, root password, DB/queue URL, raw ROS topic, `/cmd_vel`, serial, baudrate, WAVE ROVER, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, or robot/internal technical fields.

This review handoff gate is not a control grant. The package only means “ready or blocked for human review handoff”; it does not prove real iPhone/Android device behavior, production app readiness, a real PWA install prompt or user choice, O5 external proof, HIL, dropoff success, cancel completion, or delivery success. Missing real device materials, production app evidence, or real PWA install prompt/user choice keeps Start Delivery, Confirm Dropoff, and Cancel fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, and review handoff checks. ACK, HTTP accepted, receipt, intake package, decision package, and review handoff package remain accepted/processing/support metadata only.

The mobile PWA now includes a first-screen “真实设备评审执行” panel. It consumes or derives `mobile_real_device_review_execution`, `mobile_real_device_review_execution_summary`, and `mobile_real_device_review_execution_package` from `mobile_real_device_review_handoff`, `mobile_real_device_review_handoff_summary`, or `mobile_real_device_review_handoff_package`. The review execution schema is `trashbot.mobile_real_device_review_execution.v1`, the summary schema is `trashbot.mobile_real_device_review_execution_summary.v1`, the copy package schema is `trashbot.mobile_real_device_review_execution_package.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_review_execution_gate`.

The review execution panel shows the review execution checklist, review result/status, evidence items readiness, operator notes, reviewer notes, blocked reason, next evidence request, redaction status, source boundary, ACK-not-delivery wording, and `not_proven`. `review_result=pending_manual_execution` or `blocked_missing_evidence` means only that a human review execution record exists or is blocked; it is not real-device acceptance and it is not a delivery, HIL, O5 external proof, or production-app signal.

The review execution package is filtered to phone-safe metadata only. It may include execution/session schema, execution session id, handoff session id, decision status, review owner/status/result, `safe_to_control=false`, evidence items readiness, operator notes, reviewer notes, blocked reason, next evidence request, review execution checklist, redaction status, sanitized linked review handoff summary, safe phone copy, recovery hint, ACK semantics, evidence boundary, source evidence boundary, and `not_proven`. It must not include token, Authorization, OSS AK/SK, root password, DB/queue URL, raw ROS topic, `/cmd_vel`, serial, baudrate, WAVE ROVER, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, or robot/internal technical fields.

This review execution gate is not a control grant. The package only means “manual review execution record”; it does not prove real iPhone/Android device behavior, production app readiness, a real PWA install prompt or user choice, O5 external proof, HIL, dropoff success, cancel completion, or delivery success. Missing real device materials, production app evidence, or real PWA install prompt/user choice keeps Start Delivery, Confirm Dropoff, and Cancel fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, and review execution checks. ACK, HTTP accepted, receipt, intake package, decision package, review handoff package, and review execution package remain accepted/processing/support metadata only.

The mobile PWA now includes a first-screen “真实设备复测请求” panel. It consumes or derives `mobile_real_device_retest_request`, `mobile_real_device_retest_request_summary`, and `mobile_real_device_retest_request_package` from `mobile_real_device_review_execution`, `mobile_real_device_review_execution_summary`, or `mobile_real_device_review_execution_package`. The retest request schema is `trashbot.mobile_real_device_retest_request.v1`, the summary schema is `trashbot.mobile_real_device_retest_request_summary.v1`, the copy package schema is `trashbot.mobile_real_device_retest_request_package.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_retest_request_gate`.

The retest request panel shows the retest checklist, missing evidence list, per-material readiness/status, owner/next action, blocked reason, rejection reason, redaction status, source boundary, ACK-not-delivery wording, and `not_proven`. `request_status=blocked_missing_evidence` or `pending_retest_materials` means only that the next real-device retest has requested materials; it is not real-device acceptance and it is not a delivery, HIL, Objective 5 external proof, PWA install prompt, or production-app signal.

The retest request package is filtered to phone-safe metadata only. It may include request/session schema, retest request id, execution session id, request status, review result/status, owner, next action, `safe_to_control=false`, material readiness, missing evidence list, blocked reason, rejection reason, retest checklist, redaction status, sanitized linked review execution summary, safe phone copy, recovery hint, ACK semantics, evidence boundary, source evidence boundary, and `not_proven`. It must not include token, Authorization, OSS AK/SK, root password, DB/queue URL, raw ROS topic, `/cmd_vel`, serial, baudrate, WAVE ROVER, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, or robot/internal technical fields.

This retest request gate is not a control grant. The package only means “next real-device retest material request”; it does not prove real iPhone/Android device behavior, production app readiness, a real PWA install prompt or user choice, Objective 5 external proof, HIL, dropoff success, cancel completion, or delivery success. Missing real device materials, production app evidence, real PWA install prompt/user choice, or Objective 5 external materials keeps Start Delivery, Confirm Dropoff, and Cancel fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, review execution, and retest request checks. ACK, HTTP accepted, receipt, intake package, decision package, review handoff package, review execution package, and retest request package remain accepted/processing/support metadata only.

The mobile PWA now includes a first-screen “真实设备现场试跑包” panel. It generates or consumes `mobile_real_device_field_trial_package`, `mobile_real_device_field_trial_package_summary`, and `mobile_real_device_field_trial_package_copy`. The package schema is `trashbot.mobile_real_device_field_trial_package.v1`, the summary schema is `trashbot.mobile_real_device_field_trial_package_summary.v1`, the copy schema is `trashbot.mobile_real_device_field_trial_package_copy.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_field_trial_package_gate`.

The field trial package automatically records only phone-safe runtime metadata: viewport CSS width/height, device pixel ratio, orientation, touch capability, max touch points, coarse pointer, display-mode, manifest link presence, service worker support/registration hint, offline shell hint, client timestamp, and an entry URL summary with query/hash removed. Manual observation fields are device type, OS/browser, production app observed, PWA install prompt observed, user choice, offline reload observed, touch target issue, visual issue, operator note, and support note.

The copy package is whitelist-only. It must keep `safe_to_control=false`, `ack_semantics=accepted_processing_only_not_delivery_success`, `evidence_boundary=software_proof_docker_mobile_real_device_field_trial_package_gate`, and `not_proven`. It must not include token, Authorization, OSS AK/SK, root password, DB/queue URL, raw ROS topic, `/cmd_vel`, serial, baudrate, WAVE ROVER, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, or robot/internal technical fields.

This field trial package is Docker/local mobile software proof only. It does not prove a real iPhone/Android device, production app, real PWA install prompt, real prompt user choice, Objective 5 external proof, HIL, dropoff success, cancel completion, or delivery success. It also does not add a control-grant condition: Start Delivery, Confirm Dropoff, and Cancel continue to be controlled only by the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, review execution, and retest request gates.

The mobile PWA now includes a first-screen “现场试跑证据复核” panel. It generates or consumes `mobile_real_device_field_trial_review`, `mobile_real_device_field_trial_review_summary`, and `mobile_real_device_field_trial_review_copy` from the prior `mobile_real_device_field_trial_package*` family. The review schema is `trashbot.mobile_real_device_field_trial_review.v1`, the summary schema is `trashbot.mobile_real_device_field_trial_review_summary.v1`, the copy schema is `trashbot.mobile_real_device_field_trial_review_copy.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_field_trial_review_gate`.

The review status must cover real device, production app, PWA install prompt, user choice, offline, touch, visual, and material redaction. Missing or unsafe material must remain `blocked` or `not_proven`; the page must not turn a field trial package into production app readiness, true PWA prompt acceptance, real device acceptance, or delivery success.

The review copy package is whitelist-only. It must keep `safe_to_control=false`, `ack_semantics=accepted_processing_only_not_delivery_success`, `evidence_boundary=software_proof_docker_mobile_real_device_field_trial_review_gate`, and full `not_proven`. It must not include token, Authorization, credential-bearing URL, raw local path, serial/UART details, WAVE ROVER parameters, ROS topic, `/cmd_vel`, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, or robot/internal technical fields.

This review gate is software proof only. It reviews material shape, missing evidence, and redaction state; it does not prove real iPhone/Android device behavior, production app readiness, a real PWA install prompt or user choice, Objective 5 external proof, HIL, true dropoff/cancel completion, true delivery, or any control release. Start Delivery, Confirm Dropoff, and Cancel continue to fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, review execution, and retest request gates.

The mobile PWA now includes a first-screen “现场试跑执行清单” panel. It generates or consumes `mobile_real_device_field_trial_runbook_execution`, `mobile_real_device_field_trial_runbook_execution_summary`, and `mobile_real_device_field_trial_runbook_execution_copy` from the prior `mobile_real_device_field_trial_review*` family. The runbook execution schema is `trashbot.mobile_real_device_field_trial_runbook_execution.v1`, the summary schema is `trashbot.mobile_real_device_field_trial_runbook_execution_summary.v1`, the copy schema is `trashbot.mobile_real_device_field_trial_runbook_execution_copy.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_field_trial_runbook_execution_gate`.

The runbook execution checklist must cover real device, production app, PWA install prompt, user choice, offline, touch, visual, and material redaction. Missing material remains an open item or `not_proven`; the page must not turn an execution checklist into real mobile acceptance, production app readiness, true PWA prompt acceptance, real cloud/4G proof, or delivery success.

The runbook execution copy package is whitelist-only. It must keep `safe_to_control=false`, `ack_semantics=accepted_processing_only_not_delivery_success`, `evidence_boundary=software_proof_docker_mobile_real_device_field_trial_runbook_execution_gate`, and full `not_proven`. It must not include token, Authorization, OSS AK/SK, DB/queue URL, credential-bearing URL, raw ROS topic, `/cmd_vel`, serial/UART, baudrate, WAVE ROVER parameters, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, or robot/internal technical fields.

This runbook execution gate is software proof only. It guides the next real-device field trial execution and evidence capture; it does not prove real iPhone/Android device behavior, production app readiness, a real PWA install prompt or user choice, Objective 5 external proof, HIL, true dropoff/cancel completion, true delivery, or any control release. Start Delivery, Confirm Dropoff, and Cancel continue to fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, review execution, and retest request gates.

The mobile PWA now includes a first-screen “现场证据记录” panel. It generates or consumes `mobile_real_device_field_trial_evidence_record`, `mobile_real_device_field_trial_evidence_record_summary`, `mobile_real_device_field_trial_evidence_record_copy`, and `mobile_real_device_field_trial_evidence_record_archive` from the prior `mobile_real_device_field_trial_runbook_execution*` family. The record schema is `trashbot.mobile_real_device_field_trial_evidence_record.v1`, the summary schema is `trashbot.mobile_real_device_field_trial_evidence_record_summary.v1`, the copy schema is `trashbot.mobile_real_device_field_trial_evidence_record_copy.v1`, the archive schema is `trashbot.mobile_real_device_field_trial_evidence_record_archive.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_field_trial_evidence_record_gate`.

The evidence record fields must cover real device, production app, PWA install prompt, user choice, offline, touch, visual, material redaction, operator note, and support note. Missing material remains an open item or `not_proven`; the page must not turn an evidence record into real mobile acceptance, production app readiness, true PWA prompt acceptance, real cloud/4G proof, or delivery success.

The evidence record copy/archive package is whitelist-only. It must keep `safe_to_control=false`, `ack_semantics=accepted_processing_only_not_delivery_success`, `evidence_boundary=software_proof_docker_mobile_real_device_field_trial_evidence_record_gate`, and full `not_proven`. It must not include token, Authorization, OSS AK/SK, DB/queue URL, credential-bearing URL, raw ROS topic, `/cmd_vel`, serial/UART, baudrate, WAVE ROVER parameters, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, or robot/internal technical fields.

This evidence record gate is software proof only. It records and archives phone-safe field observations; it does not prove real iPhone/Android device behavior, production app readiness, a real PWA install prompt or user choice, Objective 5 external proof, HIL, true dropoff/cancel completion, true delivery, or any control release. Start Delivery, Confirm Dropoff, and Cancel continue to fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, review execution, retest request, field trial review, and runbook execution gates.

The mobile PWA now includes a first-screen “现场证据 verdict” panel. It generates or consumes `mobile_real_device_field_trial_evidence_verdict`, `mobile_real_device_field_trial_evidence_verdict_summary`, and `mobile_real_device_field_trial_evidence_verdict_copy` from the prior `mobile_real_device_field_trial_evidence_record*` family or the local `mobile_real_device_field_trial_evidence_record_archive`. The verdict schema is `trashbot.mobile_real_device_field_trial_evidence_verdict.v1`, the summary schema is `trashbot.mobile_real_device_field_trial_evidence_verdict_summary.v1`, the copy schema is `trashbot.mobile_real_device_field_trial_evidence_verdict_copy.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_field_trial_evidence_verdict_gate`.

The verdict panel must show field-level missing evidence, a review verdict, the next `retest_request`, and the next `material_request`. Missing real device, production app, PWA install prompt, user choice, offline, touch, visual, or redaction material remains `not_proven`; a clear verdict never becomes a robot control grant.

The evidence verdict copy package is whitelist-only. It must keep `safe_to_control=false`, `ack_semantics=accepted_processing_only_not_delivery_success`, `evidence_boundary=software_proof_docker_mobile_real_device_field_trial_evidence_verdict_gate`, source boundary `software_proof_docker_mobile_real_device_field_trial_evidence_record_gate`, and full `not_proven`. It must not include token, Authorization, OSS AK/SK, DB/queue URL, credential-bearing URL, raw ROS topic, `/cmd_vel`, serial/UART, baudrate, WAVE ROVER parameters, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, raw record/archive payloads, or robot/internal technical fields.

This evidence verdict gate is software proof only. It reviews field trial evidence records and requests the next real-device retest/material package; it does not prove real iPhone/Android device behavior, production app readiness, a real PWA install prompt or user choice, Objective 5 external proof, HIL, true dropoff/cancel completion, true delivery, or any control release. Start Delivery, Confirm Dropoff, and Cancel continue to fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, review execution, retest request, field trial review, runbook execution, and evidence record gates.

The mobile PWA now includes a first-screen “现场复测执行” panel. It generates or consumes `mobile_real_device_field_trial_retest_execution`, `mobile_real_device_field_trial_retest_execution_summary`, and `mobile_real_device_field_trial_retest_execution_copy` from the prior `mobile_real_device_field_trial_evidence_verdict*` family. The retest execution schema is `trashbot.mobile_real_device_field_trial_retest_execution.v1`, the summary schema is `trashbot.mobile_real_device_field_trial_retest_execution_summary.v1`, the copy schema is `trashbot.mobile_real_device_field_trial_retest_execution_copy.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_field_trial_retest_execution_gate`.

The retest execution panel must turn the verdict `retest_request` and `material_request` into a phone-safe execution checklist, missing evidence summary, linked verdict reference, source boundary, ACK-not-delivery wording, and `not_proven`. It may support Product/Support scheduling for the next real-device field trial, but it is not a robot command, ACK, cursor advancement, or delivery-success signal.

The retest execution copy package is whitelist-only. It must keep `safe_to_control=false`, `ack_semantics=accepted_processing_only_not_delivery_success`, `evidence_boundary=software_proof_docker_mobile_real_device_field_trial_retest_execution_gate`, source boundary `software_proof_docker_mobile_real_device_field_trial_evidence_verdict_gate`, and full `not_proven`. It must not include token, Authorization, OSS AK/SK, DB/queue URL, credential-bearing URL, raw ROS topic, `/cmd_vel`, serial/UART, baudrate, WAVE ROVER parameters, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, raw verdict payloads, attachments, or robot/internal technical fields.

This retest execution gate is software proof only. It moves the previous verdict retest/material request into a repeatable execution checklist; it does not prove real iPhone/Android device behavior, production app readiness, a real PWA install prompt or user choice, real public HTTPS/TLS, 4G/SIM, OSS/CDN live traffic, production DB/queue, HIL, true dropoff/cancel completion, true delivery, or any control release. Start Delivery, Confirm Dropoff, and Cancel continue to fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, review execution, retest request, field trial review, runbook execution, evidence record, and evidence verdict gates.

The mobile PWA now includes a first-screen “现场验收会话” panel. It generates or consumes `mobile_real_device_field_trial_acceptance_session`, `mobile_real_device_field_trial_acceptance_session_summary`, and `mobile_real_device_field_trial_acceptance_session_copy`. The acceptance session schema is `trashbot.mobile_real_device_field_trial_acceptance_session.v1`, the summary schema is `trashbot.mobile_real_device_field_trial_acceptance_session_summary.v1`, the copy schema is `trashbot.mobile_real_device_field_trial_acceptance_session_copy.v1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_real_device_field_trial_acceptance_session_gate`.

The session input priority is explicit `mobile_real_device_field_trial_acceptance_session*`, then `mobile_real_device_field_trial_retest_execution*`, then `mobile_real_device_field_trial_evidence_verdict*`, then `mobile_current_pwa_field_trial_browser_proof*`. If all are missing, the page derives only a blocked-by-design session. The checklist must cover real device observed, production app observed, PWA install prompt observed, install/user choice observed, offline reload observed, touch target issue, visual issue, material redaction status, and operator/support note.

The acceptance session copy package is whitelist-only. It must keep `safe_to_control=false`, `ack_semantics=accepted_processing_only_not_delivery_success`, `evidence_boundary=software_proof_docker_mobile_real_device_field_trial_acceptance_session_gate`, source boundary from the selected input layer, and full `not_proven`. It must not include token, Authorization, OSS AK/SK, DB/queue URL, credential-bearing URL, raw ROS topic, `/cmd_vel`, serial/UART, baudrate, WAVE ROVER parameters, local filesystem paths, traceback, checksum, complete artifacts, raw robot responses, raw intake JSON, complete source materials, or robot/internal technical fields.

This acceptance session gate is Docker/local mobile software proof only. It turns the prior retest execution, evidence verdict, or current PWA browser proof into a repeatable field-trial acceptance session package. It does not prove real phone acceptance, production app behavior, a real PWA prompt or user choice, Objective 5 external proof, HIL, dropoff success, cancel completion, delivery success, or any control release. Start Delivery, Confirm Dropoff, and Cancel remain fail closed through the existing command safety, cloud/device/browser readiness, handoff session, operation log, action feedback, review handoff, review execution, retest request, field trial review, runbook execution, evidence record, evidence verdict, and retest execution gates.

The mobile PWA service worker caches only the static shell. It bypasses `/api/*`, `/robots/*`, command routes, ACK routes, diagnostics, and all non-GET requests with `no-store`. The offline shell does not cache, queue, or replay control requests; it shows recovery copy and keeps primary actions disabled.

The cloud-relay runtime can now host this same `mobile/web/` shell at `/` and `/index.html`, with assets at `/app.js`, `/styles.css`, `/manifest.webmanifest`, `/service-worker.js`, `/offline.html`, `/icon-192.svg`, and `/icon-512.svg`. API/probe/control routes keep priority: `/robots/*`, `/healthz`, `/readyz`, `/preflightz`, command routes, ACK routes, and unknown `/api/*` paths must not be served by static fallback. Missing static assets and path traversal attempts return phone-safe JSON 404 without local absolute paths.

The same cloud-relay handler provides phone-safe, same-origin `GET /api/status` and `GET /api/diagnostics` for the hosted shell. These two static-phone APIs do not require bearer auth, select `TRASHBOT_REMOTE_CLOUD_DEFAULT_ROBOT_ID` or `trashbot-001`, and wrap the relay store's latest `/robots/{robot_id}/status` into a safe summary when present. If the relay has no status, the phone receives `state=status_missing` and `overall_status=blocked` rather than a browser-level 404. The adapter always keeps Start Delivery, Confirm Dropoff, and Cancel fail closed with `can_collect=false`, `can_confirm_dropoff=false`, `can_cancel=false`, `phone_readiness.can_continue=false`, and `command_safety.actions.*.enabled=false`.

The evidence boundary is `software_proof_docker_cloud_hosted_mobile_web_gate`; it proves only Docker/local hosted shell routing plus phone-safe status/diagnostics adapter behavior. It does not prove real public cloud, HTTPS/TLS, 4G/SIM, real phone device/browser behavior, production app, PWA install prompt, OSS/CDN live traffic, production DB/queue, Nav2/fixed-route, WAVE ROVER, HIL, or delivery success. The adapter must not expose token, Authorization, DB/queue URL, local path, ROS topic, serial, WAVE ROVER, `/cmd_vel`, traceback, complete artifact, or any command/status/ACK robot contract change.

The mobile PWA now includes a read-only operation log panel. It first consumes `operation_log` or `phone_operation_log` when the status payload provides them. If neither field exists, it derives a minimal recent-event list only from existing phone-safe fields: `phone_readiness`, `command_safety`, `phone_task_flow_readiness`, `phone_offline_resume_readiness`, `phone_support_bundle`, and `voice_prompt_readiness`. The panel shows Chinese-first recent events, recovery hints, ACK wording, and support handoff copy, but it does not open Start Delivery, Confirm Dropoff, Cancel, ACK, cursor, or delivery-success paths.

The operation log evidence boundary is `software_proof_docker_mobile_operation_log_gate`. This proves the local/static mobile page can render phone-safe recent events, recovery hints, and support handoff entry from fixture/status payloads while keeping Start/Confirm/Cancel fail-closed. It does not prove real phone device/browser behavior, a production app, a real PWA install prompt, real cloud/4G, OSS/CDN live traffic, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, or real delivery.

Operation log content must not expose tokens, Authorization headers, OSS AK/SK, root passwords, DB or queue URLs, raw ROS topic names, `/cmd_vel`, serial devices, baudrate values, WAVE ROVER parameters, local filesystem paths, tracebacks, checksums, full artifacts, or any wording that turns ACK into delivery success.

The mobile PWA now includes an action feedback panel for Start Delivery, Confirm Dropoff, and Cancel. It shows the most recent user action, submission state, failure or blocking reason, recovery hint, `client_reference`, ACK semantics, and the evidence boundary. The panel consumes `mobile_action_receipt` or `phone_action_feedback` when the status payload provides them; if a local submit request fails, the page shows local `failed` / `blocked` copy. Local failure copy is deliberately narrow: it only says the phone/API submit layer failed or did not receive accepted/processing evidence. It does not say that the robot moved, arrived, dropped off, stopped, canceled, or completed the task.

Confirm Dropoff and Cancel now use a generic mobile action confirmation body:

```json
{
  "schema": "trashbot.mobile_action_confirmation.v1",
  "schema_version": 1,
  "source": "mobile_web",
  "action": "confirm_dropoff",
  "user_confirmed": true,
  "client_reference": "mobile_web_confirm_dropoff_...",
  "client_timestamp": "ISO-8601 timestamp",
  "safe_phone_copy": "确认投放 已由用户二次确认提交，等待 accepted/processing 证据。",
  "ack_semantics": "accepted_processing_only_not_delivery_success",
  "evidence_boundary": "software_proof_docker_mobile_action_feedback_gate"
}
```

The same schema is used for `action=cancel`. It is a phone-safe confirmation envelope only. It must not include raw ROS topic names, `/cmd_vel`, serial devices, baudrate values, WAVE ROVER parameters, token values, Authorization headers, OSS AK/SK, DB or queue URLs, local filesystem paths, complete artifacts, or checksums.

After sprint `2026.05.13_22-23_mobile-terminal-action-confirmation-gate`, Confirm Dropoff and Cancel still use the same compatible schema, but submission is guarded by the visible terminal action confirmation panel. The terminal gate evidence boundary is `software_proof_docker_mobile_terminal_action_confirmation_gate`; action feedback can still render backend or local receipts, but those receipts remain accepted/processing evidence only.

The action feedback evidence boundary is `software_proof_docker_mobile_action_feedback_gate`. This proves the local/static mobile page can render phone-safe action receipts, local submit failures, recovery hints, client references, and ACK wording from fixture/status payloads. It does not prove real phone device/browser behavior, a production app, a real PWA install prompt, real cloud/4G, OSS/CDN live traffic, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, real dropoff, real cancel completion, or real delivery.

ACK, HTTP accepted, or receipt copy must remain accepted/processing evidence only. The page must not describe those events as delivery success, dropoff success, cancel completed, Nav2/fixed-route success, WAVE ROVER movement, real cloud/4G readiness, HIL pass, or true task completion.

The mobile PWA now includes a first-screen “云中转状态” panel for cloud/preflight/DB/queue readiness. It consumes only phone-safe metadata from `phone_cloud_readiness_summary`, `mobile_cloud_readiness_summary`, `cloud_readiness_summary`, or `phone_readiness.cloud_readiness`. The panel shows overall status, preflight status, DB/queue status, `production_ready`, ACK semantics, evidence boundary, and recovery guidance in Chinese-first copy.

The cloud readiness summary schema is `trashbot.phone_cloud_readiness_summary.v1`. The local/static evidence boundary is `software_proof_docker_mobile_cloud_readiness_summary_gate`. The summary may reference upstream Docker/local proof such as `software_proof_docker_cloud_db_queue_config_gate`, but this is only a source boundary for sanitized readiness copy. It does not prove real phone device/browser behavior, a production app, real HTTPS/TLS, a public cloud entrypoint, real 4G/SIM, OSS/CDN live traffic, production DB/queue, multi-instance consistency, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, or real delivery.

When the cloud readiness summary is missing, `overall_status=blocked`, `production_ready=false`, or the backend has not explicitly set `primary_actions_enabled=true` / `safe_to_control=true`, the page keeps Start Delivery, Confirm Dropoff, and Cancel disabled. Diagnostics and Support Handoff remain visible so support can inspect or copy sanitized information without enabling motion-related controls. This gate is intentionally conservative: a present preflight or DB/queue config summary is not a user-control grant by itself.

The cloud readiness panel and fixture must not expose tokens, Authorization headers, OSS AK/SK, root passwords, DB URLs, queue URLs, credential-bearing URLs, raw ROS topic names, `/cmd_vel`, serial devices, baudrate values, WAVE ROVER parameters, local filesystem paths, tracebacks, checksums, complete artifacts, or wording that turns ACK into delivery success. ACK copy must remain accepted/processing evidence only and must not be described as real cloud readiness, production queue success, task success, dropoff success, cancel completion, HIL, or true delivery.

The mobile PWA now includes a first-screen “手机验收准备” panel for real mobile-device/browser acceptance readiness. It consumes only phone-safe metadata from `mobile_device_acceptance_readiness`, `phone_device_acceptance_readiness`, `mobile_browser_acceptance_readiness`, or the same fields nested under `phone_readiness` or `/api/diagnostics`. The panel shows viewport/touch readiness, PWA install prompt/offline readiness, diagnostics/cloud gate readiness, `production_app_ready`, recovery guidance, ACK semantics, and evidence boundary.

The device acceptance readiness schema is `trashbot.mobile_device_acceptance_readiness.v1`. The local/static evidence boundary is `software_proof_docker_mobile_device_acceptance_readiness_gate`. This proves the local/static mobile page can render a phone-safe blocked-by-design readiness summary and keep primary actions disabled when real phone/browser or production app evidence is missing. It does not prove real iPhone/Android behavior, a production app, a real PWA install prompt, real cloud/4G, OSS/CDN live traffic, production DB/queue, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, or real delivery.

When the device acceptance readiness summary is missing, `overall_status=blocked`, `safe_to_control` is not true, or the backend has not simultaneously set `primary_actions_enabled=true` and `production_app_ready=true`, the page keeps Start Delivery, Confirm Dropoff, and Cancel disabled. Diagnostics and Support Handoff remain visible because support and recovery must not require enabling motion-related controls.

The device acceptance panel and fixture must not expose tokens, Authorization headers, OSS AK/SK, root passwords, DB URLs, queue URLs, credential-bearing URLs, raw ROS topic names, `/cmd_vel`, serial devices, baudrate values, WAVE ROVER parameters, local filesystem paths, tracebacks, checksums, complete artifacts, or wording that turns ACK into delivery success. ACK copy must remain accepted/processing evidence only and must not be described as real phone acceptance, production app readiness, delivery success, dropoff success, cancel completion, HIL, or true delivery.

The mobile PWA now includes a first-screen “浏览器验收包” panel and copy button for phone/support metadata. It prioritizes `mobile_browser_acceptance_bundle`, `phone_browser_acceptance_bundle`, and `mobile_acceptance_evidence_bundle`, including the same fields nested under `phone_readiness` or `/api/diagnostics`. When those fields are missing, the page derives a blocked-by-design summary only from existing phone-safe readiness, cloud, offline, diagnostics, and command-safety fields.

The browser acceptance bundle schema is `trashbot.mobile_browser_acceptance_bundle.v1`, `schema_version=1`, and the Docker/local evidence boundary is `software_proof_docker_mobile_browser_acceptance_bundle_gate`. The bundle contains only `overall_status`, `production_app_ready`, `safe_to_control`, `viewport`, `touch_target`, `pwa_install_prompt`, `offline_shell`, `diagnostics`, `cloud_gate`, `action_gate`, `ack_semantics`, `client_timestamp`, `safe_phone_copy`, `recovery_hint`, `evidence_boundary`, and `not_proven`. It is a copyable phone/support metadata package, not a robot command, not a remote ACK, and not a production readiness grant.

When the browser acceptance bundle is missing, `overall_status=blocked`, `safe_to_control=false`, `production_app_ready=false`, or `not_proven` still includes real device/browser, production app, or real PWA install prompt, Start Delivery, Confirm Dropoff, and Cancel must fail closed. Diagnostics and Support Handoff stay available so users can reproduce the blocked state and send a sanitized summary to support. The page must state that ACK is accepted/processing evidence only and not delivery success, dropoff success, cancel completion, real cloud readiness, HIL, or true delivery.

The browser acceptance bundle and copied support text must not expose tokens, Authorization headers, OSS AK/SK, root passwords, DB URLs, queue URLs, credential-bearing URLs, raw ROS topic names, `/cmd_vel`, serial devices, baudrate values, WAVE ROVER parameters, local filesystem paths, tracebacks, checksums, complete artifacts, or wording that turns ACK into delivery success. Local/Docker fixture proof does not prove real iPhone/Android behavior, a production app, a real PWA install prompt, real cloud/4G, OSS/CDN live traffic, production DB/queue, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, or real delivery.

Evidence boundary: `software_proof_docker_mobile_web_entrypoint_gate`. This proves the static entrypoint and smoke checks exist; it does not prove production app readiness, real iPhone/Android browser behavior, real service-worker install prompt, real cloud/4G, OSS/CDN live traffic, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, or delivery success.

The local page also shows live robot location when localization is publishing. `operator_gateway` subscribes to `/amcl_pose` by default and includes `robot_pose` plus recent `robot_path` points in `GET /api/status`; without AMCL data the controls still work, but the map panel waits for pose updates.

`GET /api/diagnostics` is the minimum support package for phone UI and remote support. It reports software version, map and route version labels, latest status, last task summary, machine-readable failure fields, log references, the operator status file, the vision sample manifest reference, and phone-safe O6 summaries such as `oss_cdn_manifest`, `network_recovery_drill`, `credential_rotation`, `provisioning_audit`, `production_store_queue`, `queue_ordering_drill`, `transaction_isolation`, and `production_recovery`. It does not claim that those files exist; it gives support tools stable references to inspect.

The local browser page is phone-first and uses the API fields directly: task state, `phone_copy`, `speaker_prompt`, action permissions, robot pose/path, and diagnostics. The page is still intentionally dependency-free HTML served by `operator_gateway`; it is a usable local control surface, not a production account system.

The first screen now includes a phone readiness gate derived from `/api/status.phone_readiness`. This gate answers three user questions before raw diagnostics: can the phone continue, why not, and what should happen next. It aggregates local delivery state, action permissions, local/mock remote readiness, optional cloud preflight, optional backup/restore drill summaries, and O6 artifact summaries including `oss_cdn_manifest`, `network_recovery`, `credential_rotation`, `provisioning_audit`, `production_store_queue`, `queue_ordering_drill`, `transaction_isolation`, and `production_recovery`. It is a local/Docker software proof boundary only and must not be presented as production phone app, real phone browser/device acceptance, real cloud, real 4G, real OSS/CDN, production DB/queue, production queue ordering, production transaction isolation, production backup/disaster recovery, Nav2/fixed-route delivery, WAVE ROVER motion, or HIL proof.

The current fallback phone page also exposes `phone_task_flow_readiness` as a top-level `/api/status` field and as `phone_readiness.phone_task_flow_readiness`. The schema is `trashbot.phone_task_flow_readiness.v1`, and the evidence boundary is `software_proof_docker_phone_task_flow_readiness_gate`. It organizes the first screen and diagnostics around the user task flow:

- `connection_ready`: whether the phone can use the local control entry.
- `destination_confirmed`: the user-visible trash station or a reminder to confirm it before departure.
- `trash_loaded`: explicit user confirmation that trash has been placed on the robot; this is not automatic load detection.
- `start_delivery`: the one-tap departure gate, still controlled by action permissions and command safety.
- `status_explained`: plain-language task state such as waiting, delivering, arrived, returning, completed, failed, or needs human help.
- `help_or_diagnostics`: diagnostics and manual takeover entry. Diagnostics can remain accessible while Start/Confirm/Cancel are blocked.

`GET /api/diagnostics` now includes the same `phone_task_flow_readiness` summary so support can reproduce which first-screen step was blocked. This diagnostics copy is a support summary only; it does not make the task ready, does not trigger robot actions, and does not prove delivery success.

The same object now includes `command_safety`, a stricter browser button gate for Start Delivery, Confirm Dropoff, Cancel, and Diagnostics. `phone_readiness.can_continue` may still indicate that a user can continue observing the local fallback page, but Start/Confirm/Cancel must remain disabled when the command gate sees stale status, a pending command ACK, auth failure, cloud unreachable, malformed remote response, missing/invalid/stale diagnostic references, or a manual takeover state. Diagnostics remains accessible so support data can be collected, but the page must explain the blocking reason and must not make Start/Confirm appear green just because Diagnostics can open.

The first screen now also exposes a support handoff entry backed by `phone_support_bundle`. This is a sanitized copy/paste package for a user, family member, after-sales support, or engineering support when the task is failed, blocked, waiting for ACK, or requires human takeover. It is not a raw diagnostics dump: it reuses the same status, `phone_readiness`, `command_safety`, and diagnostics summaries, then filters tokens, Authorization headers, OSS AK/SK, root passwords, DB/queue URLs, raw ROS topic names, `/cmd_vel`, serial device names, baudrate values, WAVE ROVER parameters, local paths, tracebacks, checksums, and complete artifacts. Support/Handoff and Diagnostics remain accessible when Start/Confirm/Cancel are blocked by command safety.

The first screen also exposes `voice_prompt_readiness` at top-level `/api/status`, nested under `phone_readiness.voice_prompt_readiness`, and in `GET /api/diagnostics`. The schema is `trashbot.voice_prompt_readiness.v1`, and the evidence boundary is `software_proof_docker_phone_voice_prompt_readiness_gate`. It shows the current prompt contract, trigger state, human-help requirement, playback readiness, safe phone copy, ACK semantics, and explicit `not_proven` boundaries. This is a prompt readiness gate only: `playback_ready=false` in the local/Docker proof, ACK is accepted/processing evidence only, and the object does not prove real speaker playback, TTS, microphone wake word, real phone device, production app, real cloud/4G, OSS/CDN live traffic, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, or delivery success.

The first screen and offline shell now also expose `phone_offline_resume_readiness` at top-level `/api/status`, nested under `phone_readiness.phone_offline_resume_readiness`, and in `GET /api/diagnostics`. The schema is `trashbot.phone_offline_resume_readiness.v1`, and the evidence boundary is `software_proof_docker_phone_offline_resume_gate`. This gate combines offline shell copy, reconnect/recovery hints, stale status, pending ACK, command safety, support handoff, voice prompt boundary, and ACK semantics into one phone-safe local/Docker contract. It does not cache, forge, or send Start/Confirm/Cancel control requests while offline. Start Delivery, Confirm Dropoff, and Cancel remain disabled in the offline shell; the live page still uses `command_safety` as the final button-level blocker. Diagnostics and Support Handoff remain accessible when primary actions are blocked, so users can recover or hand off a sanitized summary without making motion claims.

`phone_offline_resume_readiness` fields:

- `schema`: `trashbot.phone_offline_resume_readiness.v1`.
- `schema_version`: integer version, currently `1`.
- `evidence_boundary`: `software_proof_docker_phone_offline_resume_gate`.
- `connection_state`: `offline`, `status_stale`, `pending_ack`, `blocked`, `manual_takeover`, `recovering`, or `online`.
- `can_resume`: true only when connection state is online and command safety allows at least one primary action.
- `primary_actions_enabled`: whether Start/Confirm/Cancel currently have any enabled action after command safety.
- `support_entry_enabled`: whether Diagnostics or Support Handoff can still be opened.
- `next_action`: recovery action such as `wait_reconnect`, `wait_for_robot_status`, `wait_for_command_ack`, `retry_cloud`, `manual_takeover`, or `continue_local_flow`.
- `safe_phone_copy`: Chinese-first user copy for the first screen or offline/resume panel.
- `recovery_hint`: user-facing recovery step.
- `ack_semantics`: ACK remains accepted/processing evidence only and is not delivery success.
- `support_handoff`: sanitized support-entry summary.
- `voice_prompt`: prompt-readiness boundary summary; local/Docker proof does not prove real playback.
- `command_safety`: compact summary of the final browser button gate.
- `offline_shell`: confirms primary actions stay disabled and no control request cache is used.
- `not_proven`: explicit non-claims including real phone device/browser, production app, real cloud/4G, OSS/CDN live traffic, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, and delivery success.

The offline/resume summary must not expose tokens, Authorization headers, OSS AK/SK, root passwords, DB/queue URLs, raw ROS topic names, `/cmd_vel`, serial device names, baudrate values, local filesystem paths, checksums, or complete artifacts.

`voice_prompt_readiness` must be Chinese-first when a Chinese prompt exists. For elevator assisted delivery, `trigger_state=requesting_floor_help` must surface:

```text
你好,好心人,.我要去1楼扔垃圾,请帮我按一下电梯,
```

The voice prompt summary is phone-safe. It must not expose tokens, Authorization headers, OSS AK/SK, root passwords, DB/queue URLs, raw ROS topic names, `/cmd_vel`, serial device names, baudrate values, WAVE ROVER parameters, local filesystem paths, tracebacks, checksums, or complete artifacts.

`phone_readiness` fields:

- `schema`: `trashbot.phone_readiness.v1`.
- `schema_version`: integer version, currently `1`.
- `evidence_boundary`: `software_proof_docker_phone_task_flow_readiness_gate`.
- `primary_state`: phone-first state such as `ready`, `local_ready_remote_status_waiting`, `waiting_for_robot_status`, `waiting_for_command_ack`, `login_required`, `remote_unreachable`, `remote_response_invalid`, `manual_takeover_required`, or `monitoring`.
- `can_continue`: whether the current phone journey has a safe next step.
- `next_action`: machine-readable next action such as `continue_local_flow`, `continue_local_or_wait_remote_status`, `wait_for_robot_status`, `wait_for_command_ack`, `check_auth`, `retry_cloud`, `contact_support`, `manual_takeover`, or `watch_progress`.
- `safe_phone_copy`: user-facing summary for the first screen.
- `recovery_hint`: user-facing next-step copy.
- `support_level`: support classification such as `phone_ready`, `local_fallback`, `remote_blocked`, `remote_waiting_ack`, or `support_required`.
- `local_delivery`: current local state plus `phone_copy` and `speaker_prompt`.
- `phone_task_flow_readiness`: task-step metadata with schema `trashbot.phone_task_flow_readiness.v1`, step list, destination summary, load-confirmation requirement, start gate, status explanation, diagnostics/help entry, ACK semantics, blocking reasons, and `not_proven`.
- `action_permissions`: copies `can_collect`, `can_confirm_dropoff`, and `can_cancel`.
- `command_safety`: browser command gate with `schema=trashbot.command_safety.v1`, evidence boundary `software_proof_docker_phone_command_safety_browser_gate`, `global_block_reason`, `ack_semantics`, `safe_phone_copy`, and per-action entries for `start`, `confirm_dropoff`, `cancel`, and `diagnostics`. Each primary action combines the old `can_*` permission with remote readiness and manifest safety. `diagnostics.enabled` may stay true while its copy explains the block.
- `phone_support_bundle`: phone-safe handoff package with `schema=trashbot.phone_support_bundle.v1` and evidence boundary `software_proof_docker_phone_support_bundle_gate`. `/api/status.phone_support_bundle`, `/api/status.phone_readiness.phone_support_bundle`, and `/api/diagnostics.phone_support_bundle` use the same builder and include `bundle_id`, `generated_at`, `support_level`, `status_summary`, `failure_summary`, `next_steps`, `ack_semantics`, `support_refs`, `safe_copy`, and `not_proven`. `safe_copy` is Chinese-first and must state that ACK means accepted/processing evidence only, not delivery success.
- `voice_prompt_readiness`: phone-safe voice prompt readiness package with `schema=trashbot.voice_prompt_readiness.v1` and evidence boundary `software_proof_docker_phone_voice_prompt_readiness_gate`. `/api/status.voice_prompt_readiness`, `/api/status.phone_readiness.voice_prompt_readiness`, and `/api/diagnostics.voice_prompt_readiness` use the same summary and include `current_prompt`, `prompt_language`, `trigger_state`, `trigger_reason`, `requires_human_help`, `playback_ready=false`, `safe_phone_copy`, `ack_semantics`, `support_refs`, and `not_proven`.
- `phone_offline_resume_readiness`: phone-safe offline/resume package with `schema=trashbot.phone_offline_resume_readiness.v1` and evidence boundary `software_proof_docker_phone_offline_resume_gate`. `/api/status.phone_offline_resume_readiness`, `/api/status.phone_readiness.phone_offline_resume_readiness`, and `/api/diagnostics.phone_offline_resume_readiness` use the same summary and include connection state, resume allowance, primary action gate, support-entry gate, next action, recovery hint, ACK semantics, offline shell boundary, and `not_proven`.
- `mobile_device_acceptance_readiness`: phone-safe real-device/browser acceptance package with `schema=trashbot.mobile_device_acceptance_readiness.v1` and evidence boundary `software_proof_docker_mobile_device_acceptance_readiness_gate`. `/api/status.mobile_device_acceptance_readiness`, compatible `phone_device_acceptance_readiness` / `mobile_browser_acceptance_readiness` aliases, nested `phone_readiness.*_acceptance_readiness`, and `/api/diagnostics.*_acceptance_readiness` may use the same summary. It includes viewport/touch readiness, PWA install prompt/offline readiness, diagnostics/cloud gate readiness, `production_app_ready`, `primary_actions_enabled`, optional `safe_to_control`, recovery hint, ACK semantics, and explicit `not_proven` boundaries.
- `mobile_browser_acceptance_bundle`: phone-safe browser acceptance package with `schema=trashbot.mobile_browser_acceptance_bundle.v1` and evidence boundary `software_proof_docker_mobile_browser_acceptance_bundle_gate`. `/api/status.mobile_browser_acceptance_bundle`, compatible `phone_browser_acceptance_bundle` / `mobile_acceptance_evidence_bundle` aliases, nested `phone_readiness.*_acceptance_bundle`, and `/api/diagnostics.*_acceptance_bundle` may use the same summary. It includes viewport, touch target, PWA install prompt, offline shell, diagnostics, cloud gate, action gate, client timestamp, safe copy, recovery hint, ACK semantics, and `not_proven`; it must stay blocked-by-design until real mobile browser and production app evidence exists.
- `mobile_recovery_decision_gate`: phone-safe recovery decision package with `schema=trashbot.mobile_recovery_decision_gate.v1` and evidence boundary `software_proof_docker_mobile_recovery_decision_gate`. `/api/status.mobile_recovery_decision_gate`, compatible `mobile_recovery_decision_summary`, nested `phone_readiness.mobile_recovery_decision_*`, and `/api/diagnostics.mobile_recovery_decision_*` may use the same summary. It includes recovery state, next action, blocking reason, support entry, safe copy, recovery hint, ACK semantics, and `not_proven`; it is a read-only support and user-decision summary, not a robot command, ACK, cursor, delivery success, dropoff success, cancel completion, production readiness, or HIL proof.
- `mobile_terminal_action_confirmation_gate`: phone-safe terminal action confirmation package with `schema=trashbot.mobile_terminal_action_confirmation_gate.v1` and evidence boundary `software_proof_docker_mobile_terminal_action_confirmation_gate`. `/api/status.mobile_terminal_action_confirmation_gate`, compatible `mobile_terminal_action_confirmation_summary`, nested `phone_readiness.mobile_terminal_action_confirmation_gate`, and `/api/diagnostics.mobile_terminal_action_confirmation_*` may use the same summary. It includes Confirm Dropoff / Cancel action availability, risk copy, client reference preview, safe copy, ACK semantics, and `not_proven`; it is a local user-confirmation gate, not a robot command, ACK, cursor, delivery success, dropoff success, cancel completion, production readiness, HIL, or real delivery proof.
- `mobile_device_evidence_capture`: phone-safe device evidence capture package with `schema=trashbot.mobile_device_evidence_capture.v1` and evidence boundary `software_proof_docker_mobile_device_evidence_capture_gate`. Compatible `mobile_device_evidence_capture_summary` and `mobile_device_evidence_package` may appear at `/api/status`, nested under `phone_readiness`, or under `/api/diagnostics`. It includes viewport, touch target, display-mode/PWA, service worker/offline shell, client timestamp, ACK semantics, safe copy, recovery hint, and `not_proven`; it is evidence collection metadata, not production app readiness, real PWA install prompt proof, robot command proof, HIL, or delivery success.
- `mobile_device_handoff_session`: phone-safe real-phone acceptance handoff package with `schema=trashbot.mobile_device_handoff_session.v1` and evidence boundary `software_proof_docker_mobile_device_handoff_session_gate`. Compatible `mobile_device_handoff_session_summary` and `mobile_device_handoff_package` may appear at `/api/status`, nested under `phone_readiness`, or under `/api/diagnostics`. It includes entry URL or safe entry summary, session id, client reference, acceptance checklist, device/browser/PWA/install prompt/offline shell/touch target/viewport observation items, sanitized `mobile_device_evidence_capture` reference, ACK semantics, safe copy, recovery hint, and `not_proven`; it is support handoff metadata, not true iPhone/Android behavior, production app proof, real PWA install prompt proof, robot command proof, HIL, or delivery success.
- `mobile_pwa_install_prompt_evidence`: phone-safe PWA install prompt evidence package with `schema=trashbot.mobile_pwa_install_prompt_evidence.v1` and evidence boundary `software_proof_docker_mobile_pwa_install_prompt_evidence_gate`. Compatible `mobile_pwa_install_prompt_evidence_summary` and `mobile_pwa_install_prompt_evidence_package` may appear at `/api/status`, nested under `phone_readiness`, or under `/api/diagnostics`. It includes install prompt capture status, user outcome, display-mode, installability, offline shell, manifest/service worker status, production app readiness, safe-to-control, sanitized handoff/device-evidence/browser-bundle references, ACK semantics, safe copy, recovery hint, and `not_proven`; it is support evidence metadata, not true iPhone/Android behavior, production app proof, real PWA install prompt proof, robot command proof, HIL, or delivery success.
- `mobile_pwa_install_prompt_event_capture`: phone-safe PWA install prompt browser-event capture package with `schema=trashbot.mobile_pwa_install_prompt_event_capture.v1` and evidence boundary `software_proof_docker_mobile_pwa_install_prompt_event_capture_gate`. Compatible `mobile_pwa_install_prompt_event_capture_summary` and `mobile_pwa_install_prompt_event_capture_copy` may appear at `/api/status`, nested under `phone_readiness`, or under `/api/diagnostics`. It includes beforeinstallprompt observed/missing/blocked, client timestamp, display-mode, platform/browser summary, prompt availability, allowlisted userChoice outcome, choice timestamp, appinstalled observed/timestamp, `safe_to_control=false`, `accepted_processing_only_not_delivery_success`, safe copy, recovery hint, and `not_proven`; it is Docker/local mobile software proof and browser event capture readiness only, not real phone acceptance, production app readiness, O5 external proof, HIL, or delivery success.
- `mobile_pwa_install_prompt_evidence_export`: phone-safe PWA install prompt evidence export package with `schema=trashbot.mobile_pwa_install_prompt_evidence_export.v1` and evidence boundary `software_proof_docker_mobile_pwa_install_prompt_evidence_export_gate`. Compatible `mobile_pwa_install_prompt_evidence_export_summary` and `mobile_pwa_install_prompt_evidence_export_copy` may appear at `/api/status`, nested under `phone_readiness`, or under `/api/diagnostics`. It exports only overall status, install prompt capture status, install prompt user choice, appinstalled status, display-mode, manifest/service worker status, client reference/timestamp, `safe_to_control=false`, `accepted_processing_only_not_delivery_success`, safe copy, and `not_proven`; it is support/acceptance metadata only, not a control grant, true phone acceptance, production app readiness, O5 external proof, HIL, or delivery success.
- `mobile_real_device_evidence_intake`: phone-safe real-device material intake package with `schema=trashbot.mobile_real_device_evidence_intake.v1` and evidence boundary `software_proof_docker_mobile_real_device_evidence_intake_gate`. Compatible `mobile_real_device_evidence_intake_summary` and `mobile_real_device_evidence_package` may appear at `/api/status`, nested under `phone_readiness`, or under `/api/diagnostics`; the page may also import a redacted JSON summary. It includes device/platform, browser family/version summary, viewport, DPR, orientation, display-mode, PWA install prompt status/user choice, production app readiness, screenshot summary, URL summary, observer note, redaction status, ACK semantics, safe copy, recovery hint, and `not_proven`; it is intake/support metadata, not true iPhone/Android behavior, production app proof, real PWA install prompt proof, robot command proof, HIL, or delivery success.
- `remote_readiness`: current local/mock remote readiness object.
- `cloud_preflight` / `backup_restore`: optional phone-safe gate summaries; missing artifacts remain `not_run` or `unknown`.
- `oss_cdn_manifest`: phone-safe diagnostic object reference summary. It uses `schema=trashbot.oss_cdn_manifest`, `schema_version=1`, `evidence_boundary=software_proof_docker_phone_manifest_consumption`, `state=ready|missing|invalid|stale`, `object_count`, `cdn_url_rule`, `safe_summary`, `retry_hint`, `updated_at`, `staleness`, and `not_proven`. It must not expose the full artifact, object keys, checksums, local paths, credentials, ROS topics, or hardware details.
- `production_store_queue`: phone-safe production DB/queue gate summary. It uses `schema=trashbot.production_store_queue_gate`, `schema_version=1`, `evidence_boundary=software_proof_docker_production_store_queue_phone_consumption`, `state=ready|missing|invalid|stale`, `store_contract_status`, `queue_contract_status`, `ordering_status`, `consistency_status`, `migration_status`, `production_ready=false`, `overall_status=blocked`, `safe_summary`, `retry_hint`, `generated_at`, `staleness`, and `not_proven`. It must not expose the full artifact, checksum, local paths, DB URLs, queue URLs, credentials, ROS topics, or hardware details.
- `queue_ordering_drill`: phone-safe Docker/local queue ordering drill summary. It uses `schema=trashbot.queue_ordering_drill`, `schema_version=1`, `evidence_boundary=software_proof_docker_queue_ordering_phone_consumption`, `state=ready|missing|invalid|stale|failed`, `ordering_invariant`, `concurrency_invariant`, `cursor_invariant`, `ack_invariant`, `adjacent_command_ids`, `observed_order`, `production_ready=false`, `overall_status`, `safe_summary`, `retry_hint`, `updated_at`, `staleness`, and `not_proven`. It must not expose the full artifact, checksum, local paths, DB URLs, queue URLs, credentials, ROS topics, hardware details, or imply true production queue ordering.
- `transaction_isolation`: phone-safe Docker/local transaction isolation drill summary. It uses `schema=trashbot.transaction_isolation_drill`, `schema_version=1`, `evidence_boundary=software_proof_docker_transaction_isolation_phone_consumption`, `state=ready|missing|invalid|stale|failed`, `scenario`, `command_a_id`, `command_b_id`, `command_a_ack_state`, `command_b_ack_state`, `terminal_ack_ids`, `cursor_before`, `cursor_after_interleaving`, `cursor_invariant`, `ack_invariant`, `delivery_success=false`, `production_ready=false`, `overall_status`, `safe_summary`, `retry_hint`, `updated_at`, `staleness`, and `not_proven`. It must not expose the full artifact, checksum, local paths, DB URLs, queue URLs, credentials, ROS topics, hardware details, or imply true production transaction isolation.
- `production_recovery`: phone-safe Docker/local production backup/disaster recovery gate summary. It uses `schema=trashbot.production_recovery_gate`, `schema_version=1`, `evidence_boundary=software_proof_docker_production_recovery_phone_consumption`, `state=ready|missing|invalid|stale|failed|blocked`, `local_backup_restore_status`, `recovery_drill_status`, `production_backup_policy_status`, `disaster_recovery_status`, `state_backend_status`, `db_queue_status`, `multi_instance_status`, `retention_status`, `restore_objective_status`, `ack_semantics`, `production_ready=false`, `overall_status=blocked`, `safe_summary`, `retry_hint`, `updated_at`, `staleness`, and `not_proven`. It must not expose the full artifact, checksum, local paths, DB URLs, queue URLs, backup paths, credentials, ROS topics, hardware details, or imply true production backup/disaster recovery.
- `not_proven`: explicit list of product or hardware capabilities this local gate does not prove.

`oss_cdn_manifest.state=ready` only means the phone/API can consume a sanitized diagnostic reference summary generated from a local manifest artifact. It does not prove real OSS upload, STS issuance, CDN origin fetch, real cloud, real 4G/SIM, Nav2/fixed-route delivery, WAVE ROVER motion, delivery success, or HIL. `missing`, `invalid`, and `stale` must keep the first screen out of a green ready state and show ordinary user copy such as "诊断对象引用缺失。", "诊断对象引用损坏。", or "诊断对象引用已过期。" plus a retry hint to refresh or regenerate references.

ACK remains command processing evidence only. A remote ACK may make `remote_readiness.degradation_state=ok`, but the phone must keep reading delivery status for `completed`, `failed`, `needs_human_help`, or elevator-assist states. The UI text must explain that ACK means command accepted or processing evidence only; it does not prove delivery success, Nav2/fixed-route success, WAVE ROVER movement, real cloud/4G, or HIL.

## Local Mobile Web Browser Acceptance Gate

The current browser acceptance gate targets the standalone `mobile/web/` PWA, not the older onboard fallback operator gateway. It starts a lightweight static HTTP server, serves `mobile/web/`, and uses `mobile/fixtures/mobile_web_status.fixture.json` as the phone-safe response for both `/api/status` and `/api/diagnostics`:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 pc-tools/evidence/phone_browser_acceptance_gate.py --output-dir sprints/2026.05.14_02-03_mobile-current-pwa-browser-proof-refresh/evidence
```

The gate opens the served PWA in a real local Chromium-family browser through Chrome DevTools Protocol and checks `390x844` plus `768x900` viewports. It verifies primary Start/Confirm/Cancel buttons remain disabled, Diagnostics and Support Handoff remain available, the current first-screen panels are present, ACK copy remains accepted/processing evidence only, interactive hit areas are at least 44 CSS px, key first-screen elements avoid horizontal overflow and unreasonable overlap, and visible text does not leak token, Authorization, OSS AK/SK, DB/queue URL, ROS topic, `/cmd_vel`, serial, baudrate, WAVE ROVER, local paths, traceback, checksum, or complete artifact wording.

The current field-trial browser proof explicitly covers the three-step primary journey, recovery decision, terminal action confirmation, device evidence capture, handoff session, PWA install prompt evidence, real-device retest request, the complete field-trial chain, browser acceptance bundle, Diagnostics, Support Handoff, and ACK copy. The field-trial chain includes `mobile_real_device_field_trial_package*`, `mobile_real_device_field_trial_review*`, `mobile_real_device_field_trial_runbook_execution*`, `mobile_real_device_field_trial_evidence_record*`, `mobile_real_device_field_trial_evidence_verdict*`, `mobile_real_device_field_trial_retest_execution*`, and `mobile_real_device_field_trial_acceptance_session*`. Successful runs write `mobile_current_pwa_field_trial_browser_390x844.json/png`, `mobile_current_pwa_field_trial_browser_768x900.json/png`, and `mobile_current_pwa_field_trial_browser_acceptance_summary.json` into the sprint `evidence/` directory. The current evidence boundary is `software_proof_docker_mobile_current_pwa_field_trial_browser_proof_gate`; `software_proof_docker_mobile_current_pwa_retest_browser_proof_gate`, `software_proof_docker_mobile_current_pwa_browser_proof_refresh_gate`, and the older `software_proof_docker_mobile_web_browser_proof_gate` remain only as compatibility/source boundary fields in summary output.

This proves a real local Chromium-family browser rendered the current dependency-free PWA against a phone-safe fixture and made the field-trial packages visible/copyable while primary actions stayed fail closed. It keeps `safe_to_control=false`, `accepted_processing_only_not_delivery_success`, `not_proven`, whitelist-only copy, and Start/Confirm/Cancel fail closed. It does not prove a real iPhone/Android device, production app, real PWA install prompt or user choice, real cloud/4G, OSS/CDN live traffic, production DB/queue, O5 external materials, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, dropoff/cancel completion, or delivery success.

## Cloud-Hosted PWA Installability Gate

`pc-tools/evidence/cloud_hosted_pwa_installability_gate.py` verifies the same `mobile/web/` PWA through a Docker/local `cloud-relay` hosted URL. It starts the relay, requests hosted static routes, and writes `cloud_hosted_pwa_installability_summary.json` plus 390x844 and 768x900 browser evidence into the sprint evidence directory. The evidence boundary is `software_proof_docker_cloud_hosted_mobile_pwa_installability_gate`.

The manifest check covers `name`, `short_name`, `start_url`, `scope`, `display=standalone`, theme/background color, icons, `evidence_boundary=software_proof_docker_mobile_web_entrypoint_gate`, and `installability_evidence_boundary=software_proof_docker_cloud_hosted_mobile_pwa_installability_gate`. The hosted response header must still show `software_proof_docker_cloud_hosted_mobile_web_gate`, because the relay is the serving surface while installability is the browser/PWA acceptance gate layered on top.

The service worker check proves static shell and dynamic control traffic stay separated: `/api/*`, `/robots/*`, commands, ACK, diagnostics, and every non-GET request bypass cache with `no-store`; the script must not use offline queue/replay storage for control requests. The offline shell keeps Start Delivery, Confirm Dropoff, and Cancel disabled and only explains reconnect/recovery. The browser check keeps 390x844 and 768x900 viewports, verifies Start/Confirm/Cancel fail closed, Diagnostics and Support remain available, ACK copy is not delivery success, the browser evidence bundle is visible/copyable, key touch targets are at least 44 CSS px, and the layout has no horizontal overflow.

This is Docker/local cloud-hosted browser/PWA software proof only. It is not a real iPhone/Android device test, not a production app, not a real PWA install prompt, not real HTTPS/TLS public ingress, not real 4G/SIM, not OSS/CDN live traffic, not production DB/queue proof, not Nav2/fixed-route delivery, not WAVE ROVER/HIL, and not real delivery success.

## Local PWA Installability Gate

The local operator fallback page now exposes a minimal PWA shell for Docker/local software proof:

- `GET /manifest.webmanifest` returns `name`, `short_name`, `start_url`, `scope`, `display`, `theme_color`, `background_color`, and icon entries. `start_url` and `scope` stay on the operator shell and do not point to `/api/*`.
- The HTML head includes viewport, theme color, description, Apple mobile web app title/capable/status-bar meta, and a manifest link.
- `GET /service-worker.js` registers a static shell cache for `/`, `/index.html`, `/offline.html`, and `/manifest.webmanifest` only.
- The service worker treats `/api/*`, `/robots/*`, command routes, ACK routes, diagnostics, and every non-GET request as dynamic control traffic and fetches them with `cache: 'no-store'`.
- `GET /offline.html` is a phone-safe offline shell. It says the phone is disconnected and reconnect is required; Start Delivery, Confirm Dropoff, and Cancel remain disabled.

The evidence boundary is `software_proof_docker_phone_pwa_installability_gate`. It does not prove a production app, real iPhone/Android installation prompt, real phone device Safari/Chrome behavior, real cloud, real 4G/SIM, production account system, OSS/CDN traffic, Nav2/fixed-route delivery, WAVE ROVER motion, HIL, or delivery success.

For H2 elevator assisted delivery dry-runs, `GET /api/status` and `GET /api/diagnostics` may include an `elevator_assist` object. Older clients can ignore it. New clients should treat it as the machine-readable source for elevator UI:

- `enabled`: boolean; false means normal single-floor delivery UI.
- `mode`: expected to be `dry_run` until controlled real-world validation exists.
- `state` / `phase`: current elevator sub-state or failure reason.
- `requires_human_help`: true when the phone should ask for operator or bystander help.
- `reason`: machine-readable explanation such as `door_timeout`, `target_floor_unconfirmed`, or `unsafe_to_exit`.
- `target_floor`: target floor label when known.
- `phone_copy`: user-facing copy for the phone.
- `speaker_prompt`: prompt contract for a future speaker/TTS layer; the local gateway does not play audio.
- `evidence`: dry-run evidence such as `door_open`, `door_closed_or_unknown`, `inside_elevator`, `target_floor_confirmed`, `target_floor_unconfirmed`, `safe_to_exit`, or `unsafe_to_exit`.
- `events`: ordered elevator sub-state events when a task record provides them.

When `phase=requesting_floor_help`, `speaker_prompt` must be:

```text
你好,好心人,.我要去1楼扔垃圾,请帮我按一下电梯,
```

## Phone Status And Speaker Prompt Contract

`operator_gateway` emits these strings in every status-style JSON payload as `phone_copy` and `speaker_prompt`, so phone UI and speaker/voice layers share the same state contract.

| State | Phone copy | Speaker prompt |
| --- | --- | --- |
| `waiting_for_trash` | Waiting for trash. Place trash on the robot, then start delivery. | Please place trash on the robot. |
| `loaded_and_ready` | Trash is loaded. Ready to deliver. | Trash loaded. Preparing to depart. |
| `delivering` | Delivering to the selected trash station. | Delivering trash now. |
| `waiting_elevator_open` | 已到电梯厅，等待电梯开门。 | 等待电梯开门。 |
| `requesting_floor_help` | 已进入电梯，正在请求帮忙按楼层。 | 你好,好心人,.我要去1楼扔垃圾,请帮我按一下电梯, |
| `waiting_target_floor` | 正在等待目标楼层，请保持通道安全。 | 正在等待目标楼层。 |
| `target_floor_unconfirmed` | 未确认目标楼层，请人工确认。 | 未确认目标楼层，需要人工协助。 |
| `unsafe_to_exit` | 目标楼层出口不安全，需要人工接管。 | 需要人工协助。 |
| `resume_delivery` | 已驶出电梯，继续送往垃圾站。 | 已驶出电梯，继续送垃圾。 |
| `arrived_at_station` | Arrived. Remove or dispose of the load, then confirm dropoff. | Arrived at the trash station. Please remove the trash. |
| `returning` | Dropoff confirmed. Returning or waiting for the next task. | Dropoff confirmed. Returning now. |
| `completed` | Task completed. | Task completed. |
| `canceling` | Cancel request sent. Waiting for the robot to stop. | Cancel request sent. |
| `canceled` | Task canceled. The robot is stopped or returning to standby. | Task canceled. |
| `failed` | Task failed. Check diagnostics or request help. | Task failed. Please check the phone. |
| `needs_human_help` | Human help is required. Follow the shown instruction. | Human help is required. |

## 4G Remote MVP

The first 4G path is a robot-side outbound polling bridge, not a production account system. Start it only when needed with `remote_bridge:=true` and a mock or future `remote_cloud_base_url`; the default launch value is off.

The mock-cloud command set mirrors the local user journey:

- `collect` starts the behavior collection action.
- `confirm_dropoff` submits the manual dropoff confirmation.
- `cancel` requests cancellation of the active collection.

The robot posts status and command acknowledgements back to the cloud endpoint so the same flow can be tested offline before choosing a real cloud provider.

The local mock cloud may be started with a configured persistence path such as `mock_cloud_state_path`. When this path is empty, the mock keeps the old in-memory behavior. When it is set, the mock writes only command queue, ACK, status and summary counters to a local JSON proof file. The proof file must not contain bearer tokens, serial device names, ROS topic names, baudrate values, WAVE ROVER parameters, or hardware configuration fields.

Local/mock cloud endpoints can be protected with a bearer token. When the gate is enabled, the phone or robot bridge must send `Authorization: Bearer <token>`; missing or wrong tokens return a phone-safe `401` response with `auth_state=auth_failed`. The response never echoes the token, the Authorization header, raw cloud URL credentials, serial settings, hardware parameters, ROS topic names, or velocity-control details.

Phone clients should treat the remote status payload as the user-facing source of truth for local/mock remote-control readiness. `remote_ready=true` only means the current local/mock control-plane conditions allow the phone flow to continue. It does not prove real cloud hosting, HTTPS, SIM/4G connectivity, HIL, WAVE ROVER movement, Nav2/fixed-route success, or trash delivery completion. `GET /robots/{robot_id}/status`, `POST /robots/{robot_id}/status`, `POST /robots/{robot_id}/commands`, and `POST /robots/{robot_id}/commands/{command_id}/ack` may include `remote_readiness` with these fields:

| Field | Meaning for phone UI |
| --- | --- |
| `remote_ready` | true when the local/mock cloud gate, robot status freshness, and pending command state allow the phone to continue. This is not real cloud/4G/HIL proof. |
| `cloud_reachable` | true when this local mock control-plane process handled the request; it does not prove real HTTPS, SIM, or production cloud reachability. |
| `last_command_ack` | latest command id that has an ACK state, or empty when no command has been acknowledged. |
| `status_stale` | true when the robot has not posted status or the status age exceeds the mock freshness window. |
| `retry_hint` | phone-safe next action: `ok`, `wait_for_robot_status`, `wait_for_command_ack`, `check_auth`, `retry_cloud`, or `contact_support`. |
| `auth_state` | mock authentication state: `mock_not_required`, `required`, `authorized`, or `auth_failed`. |
| `degradation_state` | phone-safe readiness class: `ok`, `status_stale`, `command_pending`, `auth_failed`, `cloud_unreachable`, or `malformed_response`. |
| `safe_phone_copy` | ordinary user-facing copy for the phone UI. It must not contain raw JSON, ROS names, hardware details, credentials, or cloud URL secrets. |
| `pending_command_count` | count of queued commands without ACK. |
| `queue_persisted` | true when a local proof file is configured and queue/status/ACK can survive process restart. |

These fields explain remote control readiness only. ACK means the robot bridge accepted, ignored, or failed a command submission; it does not mean trash delivery, Nav2/fixed-route motion, WAVE ROVER movement, or HIL validation succeeded.

Recommended phone handling:

- `ok`: keep the main flow enabled.
- `status_stale`: show "正在等待小车上报最新状态，请稍后再试。"
- `command_pending`: keep destructive actions disabled until an ACK appears.
- `auth_failed`: ask the user to sign in again or check the access code.
- `cloud_unreachable`: ask the user to retry later or switch to local fallback when available.
- `malformed_response`: ask the user to contact support and attach diagnostics.

## 4G Remote Product Path

For a 4G robot, the formal phone path is cloud-mediated rather than phone-to-robot WiFi:

```text
phone web/app -> cloud API -> robot remote_bridge over outbound 4G HTTP polling -> ROS2 behavior
```

The local operator gateway remains a development and fallback entrypoint. The 4G MVP contract is documented in `docs/product/remote_4g_mvp.md`.
